# 📊 STATUS — Marble Game V2

> **Living truth.** Updated in the same session as the work — never at handoff time.
> States: ✅ **verified** (gate evidence recorded here) · 🔌 **wired** (code exists, no gate yet) · ⬜ **not started**. No fourth state, no "should work."

**Last updated:** 2026-07-11 (session 25 — **playtest polish batch 3 (pure TS/render, no WASM): (1) live camera orbit-able** (free-orbit-follow now drives live play too — `useFreeFollow = !isReplay || replayCamera==='free'` + OrbitControls mounted; drag to orbit, target locked on ball); **(2) one reflection probe + Fresnel** (removed enemy probe → env-map; player ball → `MeshPhysicalMaterial` metalness .85/rough .10/envMapIntensity 1.35 + `clearcoat 1` for the grazing-angle rim sheen); **(3) fading trails** (`Box3DPathTrails` → additive vertex-color lines, full 6s then fade 8s, clock rides `dt·timeScale`). `tsc` clean · vitest **123/123** · build **2.521MB**. Host-tree reconstruction; s21–s25 on host, **one push covers all five**.)
**Prior:** 2026-07-11 (session 24 — **playtest polish batch 2 (pure TS/render/sim-placement, no WASM): (1) particles out of the ball reflection** (new `REFLECTION_EXCLUDE_LAYER=2`; particle Points on layer 2, main cam enables it, reflection CubeCameras skip it → flicker gone, physics objects still reflect); **(2) enemy reflections + res 128→256** (2nd CubeCamera on the enemy ball); **(3) half-size + sunk obstacles** — cube 7→3.5, column 3→1.5/12→6, `CRUMBLE.scale` 4→2, all placed at `terrainHeight+half−OBSTACLES.sink` (sink 0.5m) so bases are buried; ships via `SCHEMA_VERSION` 7→8 delete-migration; 3 placement tests updated; **(4) particles obey sim time** (`timeScaleRef` → freeze on pause, dilate in slow-mo); **(5) persistent path trails** (`Box3DPathTrails.tsx` — teal/red ground `Line`s tracing each ball's whole-round path). `tsc` clean · vitest **123/123 ×2** · build **2.520MB**. Host-tree reconstruction; s21–s24 on host, **one push covers all four**.)
**Prior:** 2026-07-11 (session 23 — **playtest polish batch (pure TS/render, no WASM): (1) live ball reflections** via a per-frame `THREE.CubeCamera` (128px) so the marble mirrors the real arena, not just the env preset (`metalness .65/rough .18/envMap`); **(2) ground grid unified with the cubes** — deleted the flat 512px `useGridTexture`, floor now shares `useCubeGridTexture` (2048px, minor+major hierarchy, anisotropy 16) → dark thick + light minor lines, no distance aliasing; **(3) replay scrub + slow-mo + timer** — read-only bar → draggable scrub that `seekTo`s (boot **fast-forwards** the sim, WASM can't rewind; `fastForwardRef` mutes events during catch-up), auto slow-mo ramp to 0.12× over the last ~1.5s into the tag (`replayStore.slowmo`, 🐢 toggle), HUD `⏱ alive` + `⏳ TAG IN` + `🔴 TAGGED`; **(4) spacebar never toggles the menu** — `keydown` now `preventDefault`s + blurs a focused button on Space so jump always wins. `tsc` clean · vitest **123/123 ×3** · build **2.518 MB**. Reconstructed the host tree (`5a70002` + uncommitted s21/s22) in a cloud clone; s21+s22+s23 all on host, **one push covers all three** (Git state).)
**Prior:** 2026-07-11 (session 22 — **clutter defaults actually apply + Dev Tools relocate + varied brick debris + destructible-columns spec**. Grayson pushed s21 but *still saw few blocks* — root cause: the settings migration is `{...DEFAULT, ...saved}`, so his **persisted** `crumbleCount:4` won over the new default. Fix: **SCHEMA_VERSION 6→7** migration deletes saved `crumbleCount`+`propCount` so old saves re-adopt the bumped defaults (**crumble 12**, **props 12→24**). Also: **Dev Tools menu → bottom-left, minimized by default** (`column-reverse` opens it upward, bounded/scrollable); a new **Dev Tools → Clutter** section with wide-range live sliders (Loose Props 0–80, Crumble Blocks 0–40, 0=off) + the SettingsMenu Loose Props ceiling 40→80; **debris brick shapes varied** (shard axes now drawn from wide independent ranges → mixed cubic/slab/long rubble, still 3 seeded draws so determinism holds); and **Feature D (destructible columns → brick debris) speced into `docs/PLAN.md`** (reuses the crumble machinery, ~1 session, no new WASM). `tsc` clean · vitest **123/123 ×3** · build **2.516 MB**. Built in a fresh cloud clone of `origin/v2@5a70002` (+ uncommitted s21); **written to host, NOT yet pushed** — one local push covers s21+s22 together (see Git state).)
**Prior:** 2026-07-11 (session 21 — **crumble buff + free-orbit replay camera** (Grayson playtest asks, session-20 crumble "loved it, want more"). (1) **More crashable scenery:** `DEFAULT_CRUMBLE_COUNT` 4→**12** (Environment slider ceiling 20→**40**). (2) **Bigger bursts:** `CRUMBLE.debrisPerBlock` 9→**24** (near-triple) and the live-debris cap `maxLiveDebris` 60→**240** so ~10 blocks can shatter at once without the retire-oldest guard eating shards. (3) **New replay camera `'free'` (now the default):** a user-dragged drei `OrbitControls` orbit that stays **locked onto the player** — mouse orbits, scroll zooms, target eased onto the ball every frame so it never leaves frame. Mounted ONLY in free mode (conditional in JSX) so it never fights the live chase camera or the cinematic auto-orbit; `Chase` + cinematic `Orbit` kept as `ReplayBar` buttons. `tsc` clean · vitest **123/123 ×3** (symbolic `CRUMBLE.debrisPerBlock` refs → tests unaffected) · build **2.515 MB**. Built in a fresh cloud clone of `origin/v2@5a70002`; **written to host, NOT yet pushed** — one local push closes it (see Git state).)
**Prior:** 2026-07-11 (session 20 — **Phase P Feature C: voxel-crumble / crashable scenery** (the "fake" in-sandbox path, no WASM) + **repo hygiene** (`.gitattributes` CRLF kill + `npm audit` → **0 vulns**). Crumble blocks are static boxes that burst into seeded dynamic-sphere debris when a fast hitter (player or enemy) crashes through; F9-deterministic, occlusion-decoupled, blocks reform on round reset. `tsc` clean · vitest **122/122 ×3** (113 prior + 5 unit + 4 real-WASM smash/determinism/reset tests) · build **2.498 MB**. Built in a fresh cloud clone of `origin/v2@f82cb07`; **written to host, NOT yet pushed** — one local push closes it (see Git state). **Then went further:** proved the Box3D WASM bridge **rebuilds in-cloud, behavior-equivalent** (F1 1.767s identical) — and USED that to build the **"real" dynamic-box path**: added a `marble_box3d_create_dynamic_box` C primitive, rebuilt the wasm in-cloud, and switched crumble **debris from sphere-under-shard to real dynamic BOXES whose collider IS the shard.** `tsc` clean · vitest **123/123 ×3** · build **2.499 MB**. The shipped `box3d_bridge.wasm` is now a **cloud rebuild** (441748 B, from `erincatto/box3d@aaa795e`) — see Git state.)
**Prior:** 2026-07-10 (session 19 — Phase P props (A) + bumpy floor (B); terrain unified to one source; **SHIPPED to `origin/v2` @ `f82cb07`**.)
**Open phase / priority stack:**
1. ✅ **Velocity movement (player + enemy) — DONE & LOVED (sessions 6–7). Grayson: "holy shit it's amazing, so much more fun."** Player + enemy both velocity-driven; enemy aggressiveness via velocity drive (F1 catch 6.5s→1.767s). Drift/inertia, downhill roll, height-based jump, blend (C) baseline — all live sliders. **Legacy torque (player) + force (enemy) models RETIRED session 12 — velocity is now the sole model** (toggles, dead constants, and both code paths removed; no feel change).
2. ✅ **See-through cubes (session 8)** — obstacle cubes between camera and player fade to a ~40% transparent ghost. Analytic segment-vs-AABB occlusion (`systems/render/occlusion.ts`, unit-tested) drives `components/game-beta/CubeOcclusion.tsx`; no raycaster/BVH. **Awaiting playtest.**
3. ✅ **Particle FX structure (session 8)** — pooled render-side `systems/fx/ParticleSystem.ts` (single draw call). Emitters wired in `Box3DParticles.tsx`: speed **roll trails** (grounded + fast), **impact** bursts (wall/cube hits), **landing** bursts (hard touchdown). Sim fires cosmetic `onImpact`/`onLand` events + exposes `playerGrounded` — zero determinism impact. Extend = add an emitter call. **Awaiting playtest.**
4. ✅ **Play-feel presets (session 8)** — `PLAYFEEL_PRESETS` (Classic / Ice Rink / Arcade / Heavyweight / Predator) bundle physics+drift+downhill+jump+enemySpeed into one-click sets; nudging any knob → `custom`. Store action `applyPlayfeelPreset` + DEV "Play-Feel" dropdown. **Awaiting playtest — Grayson to judge the flavours.**
5. ✅ **Pause/gameover render-freeze (session 9)** — `<Canvas frameloop={frozen ? 'demand' : 'always'}>` in `Box3DScene`; `frozen = isPaused || gameState==='gameover'`. Stops the GPU spinning a static frame ~60×/s while paused or on the game-over screen (sim was already frozen there). **Awaiting playtest.**
6. ✅ **Countdown early-release (session 9)** — first control-key press (`w/a/s/d/space`) during the countdown breaks the player free for a head start; the enemy stays pinned at spawn until the timer hits zero. New `SimParams.freezeEnemy` (skips enemy AI+movement+tag, re-pins to spawn each step) + `earlyReleasedRef` latch in `Box3DScene.tickSim`. **Awaiting playtest.**
7. ✅ **Particle polish (session 9)** — **enemy roll trail** (red, mirrors the player trail; sim now exposes `enemyVel`/`enemyGrounded`) + **enemy ground breadcrumb** (a fading ember "scent trail" on its own pooled `ParticleSystem`/draw call — a mark dropped every `FX.enemyTrailSpacing` of grounded travel, lingers `FX.enemyTrailLife`s). Enemy render pos threaded via `enemyRenderPosRef`. Render-only, determinism untouched. **Awaiting playtest.**
8. ✅ **Live MOVEMENT + enemy sliders (session 11 — Known #6 core)** — the velocity-drive knobs are now live store keys → `SimParams` → DEV "Movement Tuning" sliders: player **Top Speed / Accel / Brake Decel / Air Control** + enemy **Speed / Reach (velUnit) / Accel (velAccel)**. Additive, defaults unchanged (sourced from `tuning.ts` so they can't drift), fall back to the constants when omitted so all prior tests stay byte-identical. **Awaiting playtest — Grayson to dial in feel/difficulty.**
9. ✅ **Game-over "your placing" row (session 13)** — the end-screen top-5 now appends a gold-highlighted `#N (You)` row (below a `⋯` divider) whenever your run ranks 6th+, so you can see where you landed vs the leaders even when you're not in the top 5. Beyond the tracked top 10 → `#11+` with the live time (honest — exact place isn't stored). Logic lives in a pure `components/ui/records.ts` (`computeYourPlacing`, unit-tested); `MenuOverlay.tsx` renders it. **Awaiting Grayson's visual playtest.**
10. ✅ **Settings→Box3D wiring / control-panel fix (session 15 — Known #6 CLOSED)** — Grayson: "that whole control panel on the right feels like it doesn't work." Root cause: the right-hand **SettingsMenu** was wired almost entirely to legacy Cannon store keys the Box3D sim never reads (`moveSpeed`/`playerTopSpeed`/`jumpForce`/`playerAirControl`/`gravity`/`friction`/`restitution`/`worldScale`), camera sliders were hardcoded-over (`6.0`/`11`), ground colours were ignored (hardcoded terrain texture), and Environment only applied at first boot (boot deps were `[heights, physicsPreset]`; restart doesn't remount, so it never rebuilt). **Fix:** rewired SettingsMenu to the real Box3D keys (velocity movement + feel + enemy drive, Physics-Feel gravity preset, Play-Feel preset), removed every inert legacy control; made the **camera** (`cameraStiffness`/`cameraOffset`) and **ground look** (`groundColor*`/`groundGridSize`) drive the scene live (defaults re-anchored to the shipped look + a v3→v4 migration resets stale Cannon values so it's look/feel-neutral); made **Environment + enemy size/weight rebuild live** (boot deps now include the arena keys, stable `seedRef` keeps the layout coherent so only the changed dimension updates); wired the **Shadows** toggle into the Canvas. **Awaiting Grayson's playtest.**
11. ✅ **Torque/force retirement + physics-preset cleanup (session 12)** — see item 1. **Physics presets resolved (Grayson chose "keep gravity variety, drop dead"):** removed the `frictionOnly` A/B preset (a no-op duplicate of `current` under velocity — friction is bypassed grounded); kept `current`/`v1Gravity`/`blend`, which carry the **gravity** variety the Play-Feel presets compose (Arcade=current light, Heavyweight=v1Gravity heavy, Classic/Ice/Predator=blend). DEV "Physics Feel" dropdown relabeled Light/Heavy/Blend and kept as the manual gravity picker.
12. ✅ **Obst-2 columns (session 14)** — tall static pillars now spawn, render, and are tunable. Sim already scattered/collided columns (Gate 2 sim work was done); this session wired the last mile: `columnCount/columnSize/columnHeight` store keys (defaults 6 / 3 / 12) → `Box3DScene` obstacles config → `MarbleSim` (which now also exposes physics-authoritative `columnSize`/`columnHeight` for the renderer, anti-drift like `cubeScale`); a second `<instancedMesh>` renders them (lavender-tinted, shared cube grid texture); **SettingsMenu → Environment** gained Column Count/Size/Height sliders (apply on restart, same as cubes — Known #5). **Columns are intentionally NOT camera-occluded** (occlusion.ts assumes a uniform cube AABB; tall pillars need non-uniform extents → backlog). **Awaiting Grayson's playtest** (dial column density/height; judge whether tall pillars blocking the camera warrants occlusion work).
13. ✅ **Obstacle orientation to terrain normal (session 15)** — cubes + columns were all facing straight up; they now tilt to the analytic terrain normal (`utils/terrain.ts` `getTerrainNormal` → per-obstacle quaternions on `MarbleSim.cubeQuaternions`/`columnQuaternions`, composed into the instance matrices; anti-drift like `cubeScale`). Tilts are gentle (headless check: avg 7.3° / max 15.5°, all normals up). **Visual tilt only** — the physics colliders stay axis-aligned (the bridge's `createStaticBox` takes no rotation; a tilted collider needs a C-bridge change + WASM rebuild → backlog). **Awaiting playtest.**
14. ✅ **See-through obstacle modes (session 15 — Option C)** — Grayson: the ghost is hard to read, "can't see the bottom edge / how I'm colliding." Added selectable reveal styles (`occlusionMode` store key, Settings → Visuals): **Ghost** (original), **Wireframe** (edges-only bounding box), **X-Ray** (back-faces only — see through the near face into the far interior wall, reads depth/collision), **Silhouette** (dark tinted see-through fill + bright edges), **Off**. `CubeOcclusion.tsx` reworked (orientation-aware restore + per-mode reveal). Cubes only (columns still un-occluded). **Awaiting playtest — pick the mode that reads best.**
15. ✅ **Leaderboard top-100 + zoom-to-your-rank (session 15)** — record cap raised 10→100 (`saveRecord`); the game-over screen now shows the top-5 then a **zoom window** (your row + immediate neighbours, `computePlacingWindow` in `records.ts`, unit-tested) so you can see who's just ahead/behind even when far down the list. Replaces the single placing row (session 13). **Awaiting Grayson's visual playtest.**
16. ✅ **Column camera occlusion (session 16)** — tall pillars now respect the same see-through modes as cubes (was the last remaining occlusion gap). Generalized `systems/render/occlusion.ts` to non-uniform boxes (`segmentIntersectsBox`/`findOccludingBoxes`; the cube fns now delegate → byte-identical, existing tests unchanged) + a new shared `components/game-beta/ObstacleOcclusion.tsx` (cubes = uniform box AABB + box reveal; columns = wide×tall×wide AABB + cylinder reveal). `CubeOcclusion.tsx` is now a thin wrapper over it. Detection uses the square-footprint box (matches the physics collider); the reveal draws the cylinder to match the s15 visual. Same `occlusionMode` drives both — no new UI. **Awaiting playtest.**
17. ✅ **Audio embodiment layer (session 16)** — the ball was silent; added **roll rumble** + **wind whoosh** (continuous, speed-driven) + **impact** (object/wall hit) + **landing** thuds, all additive/cosmetic. Pure speed→gain mapping unit-tested (`audio/movementAudio.ts`). Full audio design + backlog in **`docs/AUDIO.md`**. **Awaiting playtest — feel/volume tuning.**
18. ✅ **Physics Playground A + B (session 19 — Phase P)** — **(A) Scattered knock-around props:** `propCount` (default 12) extra dynamic spheres, seeded scatter outside the clear radius, varied radius, dropped onto the terrain, knocked around by play; rendered as faceted rubble chunks (icosahedron over the sphere collider — visual≠collider, per the house style) with per-frame interpolated + tumbling matrices. **(B) Variable/bumpy floor:** masked ≥2u roughness patches (`terrainRoughness`, default 0.6) that both the player and enemy feel; spawn ring stays smooth; **terrain unified** — `generateTerrainHeights()` now samples `utils/terrain.getTerrainHeight` so collider + render mesh + placement + downhill roll are one source. Both live in `SettingsMenu → Environment` (rebuild on change). Voxel-crumble (C) is designed in `docs/PLAN.md` Phase P but **not built** (needs the smash→debris burst next session). **Awaiting playtest — clutter density + floor roughness feel.**
19. ✅ **Voxel-crumble / crashable scenery (session 20 — Phase P Feature C, "fake" path)** — crumble blocks are solid **static boxes** (rust-red, own `crumbleCount` store key, default 4, `CRUMBLE.scale` 4u) until a hitter moving faster than `CRUMBLE.smashSpeed` (9 u/s) crashes into one; then the block is **parked out of the world** and bursts into `CRUMBLE.debrisPerBlock` (9) **dynamic-sphere debris** with seeded outward+up impulses + spin, rendered as elongated cuboid shards (visual≠collider, the house style) with high angular damping so they settle. Debris created lazily on smash (`Box3DWorld.destroyBody` added), capped at `maxLiveDebris` (60, retire-oldest), retired on fall-off, and **all cleared + blocks reformed on `resetPositions`**. Fully **F9-deterministic** (smash trigger reads sim state; every debris value drawn from the seeded RNG stream; crumble scatter drawn LAST so `crumbleCount:0` callers/tests stay byte-identical) and **occlusion-decoupled** (separate instancedMesh, not mixed into the cube occlusion arrays). Player-facing slider in **SettingsMenu → Environment** ("Crumble Blocks", rebuild-on-change). **UPGRADED to the "real" path same session:** new `marble_box3d_create_dynamic_box` C primitive (rebuilt in-cloud) → debris are now real dynamic **boxes whose collider matches the shard visual exactly** (half-extents = shard/2), no more sphere-under-box. Verified: `tsc` clean, vitest **123/123 ×3** (+5 unit, +4 real-WASM crumble, +1 dynamic-box-is-physical), build clean (2.499MB). **Awaiting playtest — crash a block at speed; judge debris drama + density + whether the real box tumble reads better.**
20. 🟡 Phase 3 perf evidence (needs Grayson at the machine — grab a perf-HUD/profiler screenshot) · audio backlog (duck under sonar, spatial impacts, expose SFX settings — see `docs/AUDIO.md`) · **remaining collider primitives** — rotated static box (→ tilted obstacle colliders match the visual tilt) + cylinder (→ round column colliders) — now buildable in-cloud via [[game-marble-wasm-cloud-build]] like the dynamic box was, each playtest-gated · **stacking/toppling crumble** — the dynamic box now exists, so real brick-stacks (blocks made of many small dynamic boxes that topple) are a possible next escalation beyond the burst-debris. **Known #6 + #7 ✅ resolved; dynamic-box primitive ✅ landed s20.**
22. ✅ **Crumble buff + free-orbit replay camera (session 21 — Grayson playtest response)** — Grayson smashed the s20 crumble blocks and loved it ("more of them"). Delivered: **(a)** more crashable scenery — `DEFAULT_CRUMBLE_COUNT` 4→**12**, Environment slider ceiling 20→**40**; **(b)** bigger bursts — `CRUMBLE.debrisPerBlock` 9→**24** (near-triple) + `maxLiveDebris` 60→**240** so a multi-block pileup shows all its shards before the retire-oldest guard; **(c)** a new **`'free'` replay camera, now the default** — a user-dragged drei `OrbitControls` orbit that stays locked onto the player (mouse orbit + scroll zoom, target eased onto the ball every frame). Mounted only in free mode so it never fights the live chase cam or the cinematic auto-orbit; `Chase` + `Orbit` kept as `ReplayBar` buttons. `tsc` clean, vitest **123/123 ×3**, build clean (2.515MB). **Awaiting playtest — judge block density + burst drama + whether the free orbit feels right (drag sensitivity / zoom clamps 4–80u).**
23. ✅ **Clutter defaults fix + Dev Tools relocate + varied brick debris (session 22 — Grayson playtest asks)** — (a) **"still seeing few blocks" root-caused + fixed:** the settings migrate is `{...DEFAULT, ...saved}`, so a persisted `crumbleCount:4` beat the new default — **SCHEMA_VERSION 6→7** now drops saved `crumbleCount`+`propCount` so old saves re-adopt the lively defaults (crumble 12, **props 12→24**). (b) **Dev Tools menu → bottom-left, minimized by default** (`column-reverse` opens it upward, bounded/scrollable) + a new **Clutter** section (live Loose Props 0–80 / Crumble Blocks 0–40, 0=off) + SettingsMenu Loose Props ceiling 40→80. (c) **Debris brick shapes varied** — shard axes drawn from wide independent ranges (cubic↔slab↔long rubble; still 3 seeded draws → determinism holds). `tsc` clean, vitest **123/123 ×3**, build 2.516MB. **Awaiting playtest — confirm blocks/props now show in force + judge the varied rubble + Dev Tools placement.**
24. ⬜ **Destructible columns → brick debris (session 22 — Feature D SPECED, not built)** — Grayson: "make all the columns destructible too, turn into brick shapes." Full design in `docs/PLAN.md` Phase P Feature D: point the existing crumble machinery at the column bodies (park-on-smash, height-distributed brick burst, lavender-tinted debris), no new WASM, ~1 session. **Next Phase P pull if playtest likes the crate crumble.**
21. ✅ **Repo hygiene (session 20 — was backlog item 20)** — (a) **`npm audit` → 0 vulnerabilities** (was 10 flagged by `npm audit` / 14 by Dependabot): `npm audit fix` bumped 40 transitive dev-tooling packages (vite/rollup/postcss/launch-editor — all **build-time only, none ship** in the single-file bundle); **no `--force`, `package.json` unchanged** (only `package-lock.json`), gate re-verified green after. (b) **CRLF phantom killed** — added `.gitattributes` (`* text=auto eol=lf`; `*.bat`/`*.ps1`/`*.cmd` `eol=crlf`; binaries marked) so `git status` stops over-reporting the ~30 false 'modified' files. **One-time cleanup still on Grayson:** run `git add --renormalize .` as its own commit after this push to flush the existing phantom (see Git state).

**Git state (2026-07-11 — session 20 SHIPPED ✅ · sessions 21+22+23+24+25 built, ⚠️ NOT pushed yet):** `origin/v2` HEAD = **`5a70002`** (session 20). **Confirmed s21 was never committed** — host HEAD is still `5a70002` with the s21 files sitting uncommitted (Grayson was running them live via Vite, which is *why* he still saw few blocks: the dev server picked up the new default but his **localStorage** kept `crumbleCount:4` — the s22 v7 migration is the real fix). **Sessions 21 + 22** were both built in a fresh cloud clone of `5a70002`, verified (tsc / vitest 123×3 / build green, 2.516MB), and written back to the host working tree — **uncommitted**. **Session 23** (this session) added four pure-TS/render changes on top (reflective ball · unified ground grid · replay scrub/slow-mo/timer · spacebar guard), verified the same way (tsc / vitest 123×3 / build 2.518MB), written to the host working tree + a rebuilt `dist/index.html`. **Session 24** then added five more pure-TS/render changes (particles out of reflection + enemy reflections + res 256, half-size/sunk obstacles + v8 migration, particles on sim-time, persistent path trails), verified (tsc / vitest 123×2 / build 2.520MB), written to host + rebuilt `dist`. **Session 25** then added three more (live camera orbit-able, one reflection probe + Fresnel clearcoat, fading path trails) — same verify. One local push now covers **all five** sessions (21+22+23+24+25). The cloud container still has no write git credential in Cowork (`GH_PAT`/Door B NOT available; only read-only `GITHUB_TOKEN`), and `Box3DScene.tsx` (~50KB) is over the OAuth-connector's ~40KB inline ceiling, so the clean single push is **Grayson's local PowerShell** (below). See project memory [[github-push-skill-notes]].

> **📌 Ship sessions 21 + 22 + 23 + 24 + 25 — one local push, in `C:\Projects-local\Game-Marble`:**
> ```powershell
> Remove-Item .git\index.lock -ErrorAction SilentlyContinue   # clears any stale 0-byte lock
> git add -A
> git commit -m "sessions 21-24: crumble/props buff + settings migration + free-orbit replay cam + Dev Tools/clutter + varied debris + Feature D spec + reflective balls (player+enemy) + unified ground grid + replay scrub/slow-mo/timer + spacebar guard + half-size/sunk obstacles + sim-time particles + persistent path trails"
> git push origin v2
> ```
> _(The `.gitattributes` renormalize from session 20 already flushed the CRLF phantom, so `git status` should be quiet — only the real files show. Confirmed this session: host `git status` showed exactly the 7 s21 files, no phantom churn.)_

**Session 21+22 changed files (uncommitted on host — `git add -A` catches them):** _s21_ — `src/systems/sim/tuning.ts`, `src/state/replayStore.ts`, `src/components/ui/ReplayBar.tsx`, `src/components/game-beta/Box3DScene.tsx`, `src/components/ui/SettingsMenu.tsx`. _s22_ — `src/state/persistence.ts` (SCHEMA_VERSION 6→7 migration + propCount default 24), `src/components/ui/UnifiedDebugMenu.tsx` (minimized default + Clutter section), `src/index.css` (`.debug-menu-wrapper` → bottom-left, column-reverse), `src/components/ui/SettingsMenu.tsx` (Loose Props ceiling 80), `src/systems/sim/MarbleSim.ts` (varied brick shard dims), `docs/{PLAN,STATUS,HANDOFF}.md`. _s23_ — `src/components/game-beta/Box3DScene.tsx` (live CubeCamera ball reflection + ground→cube grid texture + spacebar button-activation guard + replay fast-forward seek & slow-mo ramp), `src/components/ui/ReplayBar.tsx` (draggable scrub + slow-mo toggle + time-alive/countdown HUD), `src/state/replayStore.ts` (`slowmo` state), plus a rebuilt `dist/index.html`. _s24_ — `src/components/game-beta/Box3DScene.tsx` (enemy reflection + res 256 + REFLECTION_EXCLUDE_LAYER + timeScaleRef + mount path trails), `src/components/game-beta/Box3DParticles.tsx` (layer exclude + sim-time dt), `src/components/game-beta/Box3DPathTrails.tsx` (**new**), `src/systems/sim/tuning.ts` (OBSTACLES.sink + CRUMBLE.scale 2), `src/state/persistence.ts` (v8 migration + halved size defaults), `src/systems/sim/MarbleSim.ts` (sink in placement), `src/systems/sim/MarbleSim.test.ts` (3 placement assertions updated), rebuilt `dist/index.html`. _s25_ — `src/components/game-beta/Box3DScene.tsx` (live orbit camera + single reflection probe + MeshPhysicalMaterial/clearcoat player ball + enemy reverts to env-map), `src/components/game-beta/Box3DPathTrails.tsx` (additive fading trails), rebuilt `dist/index.html`. No WASM/native/test changes — pure TS/render.

**🔓 Infra unlock (session 20) — the Box3D WASM bridge rebuilds in-cloud, behavior-equivalent, AND we used it.** Proven: `emcc` (emscripten) + `erincatto/box3d` @ `aaa795e` compiled the bridge in the Cowork container; the *unchanged* build passed the full suite (**F1 = 1.767s, identical to the host build**). Then we **added the `marble_box3d_create_dynamic_box` primitive, rebuilt, and re-verified** — F1 still 1.767s, 123/123. So the shipped `public/box3d/box3d_bridge.wasm` is **now a cloud rebuild** (441748 B, `sourceRepo = erincatto/box3d@aaa795e` in the manifest) — behavior-equivalent to the old host build plus the new primitive. New Linux build script `native/build-box3d-bridge.sh` (mirrors the `.ps1`, pins the commit; equivalence gate in its header). **What this killed:** the "needs Grayson's Windows host to recompile WASM" blocker. **Remaining** collider primitives (rotated static box → tilted colliders; cylinder → round columns) are the same kind of in-cloud add, each playtest-gated. Full recipe: project memory [[game-marble-wasm-cloud-build]].

_Historical (session 17–19 SHIPPED, superseded):_ `origin/v2` reached **`f82cb07`** with all 27 real session 17–19 files verified byte-perfect (24 via the GitHub OAuth connector authored as Grayson + 3 large files via local PowerShell push).

> **📌 Pushing from Cowork is a SOLVED recipe now — do NOT re-derive it.** Default = the **GitHub OAuth connector** (`push_files` / `create_or_update_file`, commits as Grayson, works in Cowork with no token). Stage real files → **LF-normalize** → push in ≤~45KB batches → **verify each blob SHA** via `get_file_contents` → repair mismatches. Files **≥ ~40KB** exceed the connector's single-call emit → hand those to a **local `git push`**. `GH_PAT`/Door-B is **NOT** available in Cowork. Full recipe + gotchas: project memory **[[github-push-skill-notes]]**.

_Historical (session 16, superseded by the ship above):_
- `origin/v2` HEAD = **`25c0032`** ("session 15: control-panel rewire (Known #6), obstacle orientation + cylinder columns, see-through modes, top-100 leaderboard"). Local `v2` = origin/v2, **0 ahead / 0 behind** — **session 15 IS committed + pushed** (the session-15 doc's "uncommitted, Grayson to commit + push" note was stale; it landed as `25c0032`, authored via the github-push flow 2026-07-10 00:07). The 42-file working-tree diff is **pure CRLF churn** (Known #1) — `git diff --ignore-cr-at-eol` is empty, nothing real to commit there.
- Session 16 was built in a fresh cloud clone of `origin/v2@25c0032` (full-fidelity, no mount truncation), verified, and the changed source files + these docs written back to the host working tree (uncommitted — Grayson commits from PowerShell, or ask Claude to push via the github-push skill).
- **Session-16 changed files (uncommitted on host):** `systems/render/occlusion.ts`, `systems/render/occlusion.test.ts`, `components/game-beta/ObstacleOcclusion.tsx` (**new**), `components/game-beta/CubeOcclusion.tsx`, `components/game-beta/Box3DScene.tsx`, `index.css` (DEV-menu fix), `audio/SoundManager.ts`, `audio/movementAudio.ts` (**new**), `audio/movementAudio.test.ts` (**new**) (+ `docs/STATUS.md`, `docs/HANDOFF.md`, `docs/AUDIO.md` (**new**)). (Other files may show as `M` — pure CRLF churn per Known #1; real diff is only these.)
- **⚠️ Session 16 is NOT pushed yet — this is the #1 resume action.** All session-16 work (occlusion + audio + DEV-menu) is done, verified (tsc clean, vitest 87/87 ×5, build clean 2.481MB), and written to the host working tree. It was also committed in the ephemeral cloud clone (`5fe0184`) but **that clone dies with the session — the durable copy is the uncommitted files on Grayson's host disk.** The cloud push was **blocked**: this container has no write-capable git credential (built-in `GITHUB_TOKEN`/ambient auth are read-only; no `GH_PAT` was loaded this session).
- **`GH_PAT` credential (2026-07-10, session 16):** Grayson **added `GH_PAT` to the claude.ai/code environment-variables UI** this session so future pushes are hands-off. **NOT yet confirmed** — env vars only load at session START, so it can't be tested from the session that set it. **The confirmation test = the first `/github-push` in a fresh session.** See project memory [[github-push-skill-notes]] for the full credential story + the debug checklist if a fresh-session push still 403s.
- **→ Two ways to ship session 16 (pick one, do it FIRST next session):**
  1. **Fresh session, hands-off:** open a new Cowork/Claude Code session, say "push the Marble work to v2" — the new session loads `GH_PAT` and Claude pushes via the github-push skill (also confirms the credential setup).
  2. **PowerShell now (always works, no token):** in `C:\Projects-local\Game-Marble` → `Remove-Item .git\index.lock -ErrorAction SilentlyContinue; git add -A; git commit -m "session 16: column occlusion + audio embodiment layer + DEV-menu fix"; git push origin v2`.

**Resolved this run (were blockers, now fixed — don't re-diagnose):**
- ✅ **Corrupt local git index** (`fatal: index file corrupt` + stray `v2.lock.bak-…` ref) — fixed via `Remove-Item .git\index` + `git reset --mixed HEAD` (session 13). Recovery recipe saved to project memory in case it recurs.
- ✅ **`public/box3d/box3d_bridge.js` EPERM lock** (Vite crash on launch) — fixed by restoring the file from git (`attrib -R -S -H` → `Remove-Item` → `git checkout --`). It was OS-locked on the host; the git-tracked copy is clean.

**Awaiting Grayson's playtest** (code is verified; these are feel/visual verdicts, not "unverified"): the whole s15 right-hand control panel + see-through modes + obstacle tilt + cylinder columns + top-100 zoom window, and now **column camera occlusion (16)** — walk behind a tall pillar and confirm it goes see-through in your chosen mode (columns cost you an extra cube-density judgement: too many pillars occluding at once may read busy). Plus the still-open finer verdicts on sessions 8–13 (occlusion / presets / freeze / early-release / trails / Movement sliders / placing row). Phase 3 perf screenshot still pending (Grayson to grab next round).

Note: Grayson granted code-structure autonomy (see memory) — drive the redesign, keep these docs current; he still judges feel.

---

## 🚦 Working rules (the framework)

Adapted from the Tool-3dViewer-Fable rebuild framework — the lesson there was that *building outrunning verifying* is the root failure mode, regardless of whether the builder optimizes for completeness or honesty.

1. **The docs are the spec.** PRD/ARCHITECTURE define the target. Feature ideas go to PLAN Phase 3 backlog, not the code.
2. **Verify before advancing.** Every phase ends with an exit gate: concrete, checkable evidence. Phase N+1 does not open until phase N's gate is green *and recorded here*. "Wired but untested" is not done.
3. **Port the earned stuff, rewrite the cheap stuff.** Sonar tuning, AI feel constants, physics values are battle-tested data (see [TUNING.md](TUNING.md)) — carry them verbatim. Glue code can be rewritten freely.
4. **The game boots and plays after every phase.** A phase that ends with a broken game isn't done (PLAN rule of thumb).
5. **Deviations get logged**, not silently absorbed.
6. **Session protocol:** start by reading this file (it names the open phase and any red gates); end with this file accurate at the moment you stop, even mid-phase. If a gate fails and can't be fixed in-session, log it under Known issues — the next session starts there, never by opening a new phase on a red gate.

## Phase gates

| Phase | Scope | Gate (evidence required) | Status |
|---|---|---|---|
| 0 | Scaffold: folders, vitest, loop.ts | `npm run test` green (≥1 real loop test) · `npm run build` clean · game plays identically to v1 | ✅ 2026-07-02 — sandbox CI: vitest **8/8** (fixed-step count, constant dt, accumulator carry, tick order, delta clamp, interp alpha, reset, EventBus) · `tsc -b && vite build` clean, single-file `dist/index.html` 2.4MB. |
| 1 | Extract systems (AI → rules → sonar → store → loop) | Unit tests per system green · feel-invariant suite green · manual A/B vs v1 at 5173/5174 | ✅ 2026-07-06 — Verified in headless integration tests on Box3D backend: catches stationary player from 20u in 6.63s (limit is 8.0s), see Box3DWorldIntegration.test.ts. AI/Rules/Sonar unit tests pass. Plays fine in manual test (Launch - Box3D Beta.bat). |
| 2 | Time Alive & Smoke Tests | RulesSystem ticking score, top 10 persistent records, pulsing HUD timer, SimulationSmoke.test.ts green | ✅ 2026-07-03 (4) — fixed the F8 determinism bug (Known issues #3): `score` now accumulates from sim `dt` only in `RulesSystem.tick()`, `sessionSlice.ts` no longer derives it from `Date.now()`. **19/19 vitest, verified across 5 consecutive clean runs** (was flaky/failing before). `tsc -b` + build clean (630 modules, 2.4MB). |
| 3 | Performance | Perf HUD screenshot: 60fps at defaults, draws < 50, sim tick 0-alloc in profiler · WebGL fallback boots | 🔌 downgraded from ✅ (session 4). `tsc -b` + `vite build` do pass clean (630 modules, 2.4MB). No perf HUD screenshot or profiler capture exists anywhere in the repo/session history — "60fps verified" has no actual evidence attached, just the claim. |
| 4 | Art direction pass & Backlog | per-feature, defined when pulled | ⬜ |
| Obst-1 | Box3D static obstacles: cubes (scatter outside 20u spawn-clear radius, rested on terrain, physics+render from one source) | MarbleSim.test.ts green (count, clear radius, exact terrain-height placement, seed determinism) · full suite green · build clean | ✅ 2026-07-08 (4) — **35/35 vitest across 5 consecutive fresh runs**, `tsc -b` clean, `vite build` clean (2.45MB). Note: the feature code arrived as *uncommitted, undocumented work from an outside dev session* (provenance unknown, like `c793cd3`); this session audited it, fixed 3 issues (seeded RNG, cubeScale render/physics mismatch, weak placement test) before verifying. Feel/AI-vs-obstacles still needs Grayson's playtest. |
| Obst-2 | Box3D static obstacles: columns | MarbleSim + tests already support columns; gate = UI wiring + spawn config + playtest | ✅ 2026-07-09 (14) — code half done: `columnCount/columnSize/columnHeight` store keys (defaults 6/3/12) → `Box3DScene` obstacles config → `MarbleSim` (now exposes authoritative `columnSize`/`columnHeight`); second `<instancedMesh>` renders the pillars; SettingsMenu→Environment sliders. **71/71 vitest ×5** (70 prior + 1 new authoritative-dims test), `tsc -b` clean, `vite build` clean (single-file 2.47MB, 643 modules). Columns camera-occluded as of **session 16** (non-uniform AABB — uniform-AABB limitation lifted). **Playtest pending** = the last gate half. |
| P-A/B | Phase P: scattered props (A) + bumpy floor (B) | scatter/roughness determinism tests green · full suite green · build clean | ✅ 2026-07-10 (19) — vitest 113/113 ×5, `tsc -b` + build clean (2.492MB). Playtest pending (clutter/roughness feel). |
| P-C | Phase P: voxel-crumble / crashable scenery — "fake" burst path AND "real" dynamic-box debris | crumble scatter + additive-safety unit tests + real-WASM smash/no-smash/determinism/reset + dynamic-box-is-physical tests green · full suite green · build clean | ✅ 2026-07-11 (20) — vitest **123/123 ×3** (+5 unit, +4 real-WASM crumble, +1 dynamic-box), `tsc -b` + build clean (2.499MB). Smash + debris F9-deterministic; occlusion-decoupled. **UPGRADED to real path same session:** `marble_box3d_create_dynamic_box` C primitive (in-cloud WASM rebuild) → debris colliders now match the shard visual. **Playtest pending** (debris drama + density + real-box tumble feel). |

## 🎯 Feel invariants (Gate 1 measurable half)

The "feels identical to v1" criterion, made testable. Headless sim runs (scripted input, fixed seed) asserting:

| # | Invariant | Source of truth | Status |
|---|---|---|---|
| F1 | Enemy in `chase` catches a stationary player from 20u in < 8s at default tuning | v1 play data | ✅ 2026-07-09 (7) — enemy switched to the velocity drive: catch now **1.767s** (was 6.50s under the force model), still well under the 8s gate. Box3DWorldIntegration.test.ts (default params → enemy velocity model). |
| F2 | AI never re-enters `idle` after first contact (search loops forever) | EnemyAI.ts:134 "NEVER GO IDLE" | ✅ 2026-07-02 — `systems/ai/EnemyAI.test.ts` |
| F3 | `alert` → `chase` transition at exactly 0.5s of continuous visibility | ALERT_DURATION | ✅ 2026-07-02 — `systems/ai/EnemyAI.test.ts` |
| F4 | Losing line-of-sight in `chase` → `search` within one tick; first waypoint is velocity-projected (≤ 15u ahead) | EnemyAI.ts waypoint gen | ✅ 2026-07-02 — `systems/ai/EnemyAI.test.ts` |
| F5 | Sonar closing-channel pitch at distance ≤ 10u (audioSolidDistance) reaches max pitch (300Hz base + modulation 4) | TUNING.md audio block | ✅ 2026-07-02 — `systems/sonar/SonarSystem.test.ts` |
| F6 | Sonar strategy "drone" produces continuous tone; "pulse" produces discrete pings; Respects closing/opening volume split (1.0 / 0.3) | SoundManager port | ✅ 2026-07-02 — verified via SonarSystem |
| F7 | Restart returns to `countdown` in < 500ms with zero `window.location.reload` calls | PRD success criteria | ✅ 2026-07-02 — verified in memory restart transition |
| F8 | Same seed + same scripted input ⇒ same tagged-time within tolerance (determinism smoke) | ARCHITECTURE §4 | ✅ 2026-07-03 (4) — fixed: `score` is now purely `dt`-accumulated in `RulesSystem.tick()`, single writer. `SimulationSmoke.test.ts` green across 5 consecutive fresh runs (was failing before, see Known issues #3) |
| F9 | Same scripted input ⇒ **bit-identical** physics trajectories over 300 fixed steps (full-sim determinism incl. WASM physics; prerequisite for backlogged replay recordings) | ARCHITECTURE §4 | ✅ 2026-07-06 (3) — `Box3DWorldIntegration.test.ts` F9: two fresh WASM worlds, 300 scripted steps, positions compared with exact `toBe`. Caveat resolved 2026-07-08 (4): all sim randomness (AI search waypoints + obstacle scatter) now draws from one seeded mulberry32 stream (`systems/sim/rng.ts`, `config.seed`, recorded on `sim.seed`). Fixed default seed in tests; per-boot random seed in Box3DScene (persist it when building replays). |

## Deviations from docs

| Date | Deviation | Why |
|---|---|---|
| 2026-07-02 | Headless Vitest Integration Smoke Test over Playwright | Playwright introduces heavy dependencies (browsers/nodes) and is token-heavy. Pure TypeScript simulation smoke tests verify loop and state machines headlessly in < 30ms with zero overhead. |
| 2026-07-06 (3) | Removed invisible smoke-test colliders from gameplay worlds (`Box3DWorld.clearBodies()`, called by `MarbleSim`) | The 4x0.5x4 smoke-test floor (top at y=0) and r=0.35 sphere from `Box3DWorld.reset()` previously survived under the game terrain — an unintentional invisible ledge wherever terrain dips below 0, plus a stray dynamic body falling through the player spawn column. Behavior-affecting but almost certainly a latent bug, not tuned feel. Needs Grayson's playtest confirmation. |
| 2026-07-06 (3) | Box3D beta gameplay constants centralized in `systems/sim/tuning.ts`, which **diverges from docs/TUNING.md v1 values** (gravity -9.81 vs -22.5, jump 7.5 vs 5, topSpeed 22 vs 20) | Both value sets are real: TUNING.md holds v1/Cannon feel, tuning.ts holds the playtested Box3D beta feel. Neither was silently "fixed." Reconciling them is the Phase E parity decision. |
| 2026-07-08 (6) | **Player movement model changed from torque-driven to velocity-driven (`DEFAULT_MOVEMENT_MODEL = 'velocity'`).** The torque model (input=spin, friction converts to motion) perpetually slipped regardless of tuning — Grayson's "100 RPM, barely moving." Velocity model drives horizontal velocity directly and slaves spin to it (roll without slip). | Approved by Grayson (autonomy grant + explicit vision: "1:1 with the ground"). Torque path retained behind a DEV toggle for A/B, not deleted. **(Retired session 12 — velocity is the sole model.)** Note: velocity model largely bypasses `PLAYER.friction`/`torque`/`directionChangeBoost` and the physics-preset friction lever while grounded — those still matter for the torque model and for airborne physics. |
| 2026-07-09 (7) | **Enemy movement model → velocity (`DEFAULT_ENEMY_MOVEMENT_MODEL = 'velocity'`)**, mirroring the player. `applyEnemyVelocityControl` drives enemy velocity toward the AI heading at `enemySpeed·stateMult·ENEMY.velUnit`, spin slaved. Legacy force path kept behind a DEV toggle. **(Retired session 12.)** | Grayson's request ("enemy running the same idea"). Doubles as the aggressiveness pass — chase now ~19.5 u/s, F1 catch 6.5s→1.767s. |
| 2026-07-09 (7) | **Baseline physics feel set to blend (C):** `DEFAULT_SETTINGS.physicsPreset = 'blend'` (gravity -15). `DEFAULT_PHYSICS_PRESET` (headless fallback) left at 'current' so WASM tests that `world.reset()` at -9.81 stay gravity-consistent. | Grayson: "I like blend C … both been really good." His persisted localStorage value still wins — confirm C is selected. Under the velocity model only gravity (fall/jump/downhill) differs between presets; friction is bypassed while grounded. |
| 2026-07-09 (7) | **Jump is now height-derived:** on jump, vertical velocity is set directly to `sqrt(2·|g|·jumpHeight)` (mass-free, exact) instead of a fixed impulse. Default `PLAYER.jumpHeight = 1.8` (clears the enemy ball, can't summit the 7u cubes). Live `jumpHeight` slider. | Grayson: "default jump a little strong … just enough to jump over the other player." Old impulse gave a ~10u leap. Keeps jump *height* constant across gravity presets. `PLAYER.jumpImpulse`/preset `jumpImpulse` retained (torque path + preset contract) but unused by the velocity jump. |
| 2026-07-09 (7) | **Coast/drift + downhill roll added to the velocity model.** On release (no input) the ball coasts by exponential drag + a small constant floor (inertia glide, settles to rest on flat); on slopes it accelerates downhill by `g·sinθ · downhillRoll`, slope read from the **analytic `getTerrainHeight` gradient** (the WASM heightfield raycast returns a flat up-normal — see probe). Live `playerDrift` + `downhillRoll` sliders. | Grayson: "add drift … ball glides for a bit … and slowly roll downhill." Exponential (not fixed-rate) decay is required so downhill roll survives the coast damping. Downhill uses terrain gradient, so it does NOT roll off cube tops (acceptable). |
| 2026-07-09 (15) | **Camera + ground look moved from hardcoded to live store settings**, with `DEFAULT_SETTINGS` re-anchored to the previously-hardcoded values (`cameraStiffness` 3→6, `cameraOffset` 15→11, ground `#70b348/#3e6b1f/176` → `#1a4d2e/#4f772d/64`) and a v3→v4 migration that resets those stale Cannon-era persisted keys. | Making the dead SettingsMenu sliders actually work required the sim to read the store. Re-anchoring defaults + migrating keeps it look/feel-neutral (a fresh install and Grayson's existing save both render identically to before), while the sliders now respond. |
| 2026-07-09 (15) | **Obstacles tilt to the terrain normal VISUALLY only** — per-obstacle quaternions drive the render instance matrices; the physics `createStaticBox` colliders stay axis-aligned. | The bridge's `createStaticBox` takes no rotation, and a tilted collider needs a C-bridge change + WASM recompile (out of sandbox scope, like Known #7 was). Slopes are gentle (≤~15°) so the visual/collider mismatch on a corner is minor for scattered scenery. A rotated-collider bridge is backlogged. |
| 2026-07-09 (15) | **Environment/enemy settings now rebuild the world live** (added to the boot dep array) instead of apply-on-restart. | Grayson couldn't change the environment even after restarting — restart doesn't remount `Box3DScene`, so the first boot's obstacles were frozen forever. Live rebuild (stable `seedRef` keeps layout coherent) is the fix; it resets the round on change, which is expected for an arena edit. |
| 2026-07-10 (16) | **Column camera occlusion detects against the square-footprint box AABB, not the round cylinder** — a column is a cylinder visually but a box collider physically; occlusion tests the box (columnSize × columnHeight × columnSize). | Matches the physics collider exactly (which is also a square box), so occlusion is collider-accurate. The only over-fade is the ~21% of the footprint in the box corners outside the round visual — the reveal there just triggers slightly early, never late, so the ball is never hidden. A true cylinder occlusion test isn't worth the complexity while the collider itself is still a box. |
| 2026-07-10 (19) | **Terrain unified to ONE source (`utils/terrain.getTerrainHeight`).** `generateTerrainHeights()` moved out of `Box3DScene` into `terrain.ts` and now samples `getTerrainHeight` at each grid vertex's world position, replacing the hand-copied second formula. | The two formulas had a ~0.5-cell (~≤0.25u) offset; unifying shifts the base terrain imperceptibly AND makes obstacle placement (getTerrainHeight+half) rest EXACTLY on the collider surface instead of ~0.25u off. Removes a real desync foot-gun so the Feature-B roughness lands on collider + mesh + sim consistently. Playtest confirms the ground looks unchanged. |
| 2026-07-10 (19) | **Props are dynamic SPHERE colliders with a faceted-chunk (icosahedron) VISUAL** — the visual tumbles with the body but the collision shape is a sphere. | The bridge's only dynamic primitive is a sphere; a true blocky/tumbling collider needs a dynamic-box bridge primitive + WASM rebuild (backlogged as Feature C "real" path). Visual≠collider is the established house style (columns-as-cylinders, tilted obstacles). |
| 2026-07-10 (19) | **Props are physical bodies the enemy's raycasts can hit** (vision / avoidance / ground probe) — the bridge `worldRaycast` has no body filter, so a prop between enemy and player briefly blocks line-of-sight ("soft cover") and the enemy swerves around rolling props. | Can't cheaply exclude props from rays without bridge work. Kept the default count modest (12) + radius small so it reads as emergent cover, not broken AI. Dial `propCount` to 0 to remove entirely. Tests omit propCount → 0 props → all prior sim/F1/F9 evidence byte-identical. |

## Known issues

| # | Issue | Impact |
|---|---|---|
| 1 | **Sandbox mount serves stale/truncated file views** | Workaround: CI runs copy sources to sandbox-local scratch dir and rewrite suspect files there; host files are authoritative. Recurred hard 2026-07-08: freshly edited files served with current content but truncated at the *pre-edit byte length* (metadata updated, content pages stale); repaired scratch copies by appending known tails, host verified intact via direct reads. |
| 2 | **File deletion (`rm`/`unlink`) on the mounted folder is blocked** | Renamed file to `.DELETE_ME` suffix and hand the actual deletion to Grayson. |
| 3 | ~~**Score/tagged-time computed from `Date.now()` wall-clock instead of sim `dt`**~~ — **FIXED 2026-07-03 (session 4).** Was: both `RulesSystem.ts` (`playing` tick) and `state/sessionSlice.ts` (`setGameState` on `playing`/`gameover`) independently derived `score` from `Date.now() - startTime`, with `sessionSlice.ts`'s `gameover` transition silently overwriting `RulesSystem`'s value with a second, later `Date.now()` call. | Fix: `RulesSystem` now owns a private `playElapsed` accumulator driven only by `dt`, and is the sole writer of `score`. `sessionSlice.ts` only resets `score` to 0 on entering `playing`; `startTime` is kept but demoted to an informational timestamp only (never read back for scoring). Verified: 19/19 vitest across 5 consecutive fresh-sandbox runs, `tsc -b`+build clean. |
| 4 | **Player ground traction feels off in Box3D beta** (Grayson playtest 2026-07-08): ball spins up fast but is slow to grip and translate — wheel-spin before the right direction takes hold. | Leading hypothesis: `tuning.ts` gravity is **-9.81 vs v1's -22.5** (logged TUNING.md deviation) → normal force ~2.3× lower → friction traction ~2.3× weaker while input torque is unchanged. **Session 5 built an A/B tool** (`systems/sim/tuning.ts` `PHYSICS_PRESETS` + DEV TOOLS "Physics Feel" dropdown): `frictionOnly` (μ only, μ 0.6/0.5→0.9/0.8), `v1Gravity` (gravity -22.5, jump rescaled 7.5→11.3), `blend` (gravity -15, jump 9.3, μ 0.75/0.65). Grayson to A/B → winner becomes the Phase E TUNING reconciliation. **Applies on arena rebuild** (friction baked at body creation — same as #5); the dropdown change triggers the rebuild via the boot effect's dep array. **VERDICT 2026-07-08 (5): all three still too slippery; C (blend) gave the most authority but ball still spins ~100 RPM while barely translating.** Root cause is the *movement model*, not tuning: input applies TORQUE and relies on contact friction to convert spin→translation, so torque (18) + directionChangeBoost (4.5) perpetually exceed the grip ceiling → permanent slip. Tuning values can't fix this. **PLAN: rewrite to a velocity-driven (hybrid) model** — grounded input drives linear velocity directly (accel toward heading, top-speed cap, damping), spin slaved to motion (ω = v/r, no slip); airborne/collisions hand back to Box3D physics. Vision (Grayson): "1:1 with the ground, influence the ball from inside with energy." The A/B presets become a fallback / can be retired once the new model lands. **IMPLEMENTED session 6:** `MOVEMENT` in tuning.ts + `applyVelocityControl` in MarbleSim (grounded: accel horizontal velocity toward input·topSpeed, ω=(vz/r,0,-vx/r) slaved spin, no slip; airborne: physics + light nudge). Live-toggle via `movementModel` store key + DEV TOOLS dropdown (default `velocity`). Verified 42/42 + 2 new velocity tests (top speed, no-slip ω=v/r, decel). **Awaiting Grayson's feel verdict** on velocity vs the old torque toggle; tune `MOVEMENT.accel/decel/topSpeed` next. |
| 6 | **Box3D physics is decoupled from the settings store** (found session 5): the old "Tuning Preset" v1/v2 dropdown is inert — `V1_PRESET`/`V2_PRESET` are byte-identical to `DEFAULT_SETTINGS`, and `MarbleSim` reads gravity/friction from hardcoded `tuning.ts`, never from the store (store's `gravity:-22.5/friction:0.35` only ever fed the old Cannon path in `components/game/`). New `physicsPreset` selector is wired straight into the Box3D path and bypasses this. Full settings→Box3D repair deferred (Grayson chose minimal scope). **Session 11: the velocity-drive knobs are now exposed** — player `moveTopSpeed`/`moveAccel`/`moveBrakeDecel`/`moveAirControl` + enemy `enemyVelUnit`/`enemyVelAccel` are live store keys threaded through `SimParams` (DEV "Movement Tuning" sliders). The remaining decoupled items are the legacy Cannon-era store fields (gravity/friction/moveSpeed/etc.) that only ever fed `components/game/`. **RESOLVED session 15:** the player-facing SettingsMenu was rewired to the real Box3D keys and every inert legacy control removed; camera + ground look now drive the scene live; Environment/enemy rebuild live; Shadows wired. The dead store fields (`moveSpeed`/`jumpForce`/`playerTopSpeed`/`gravity`/`friction`/`restitution`/`worldScale`/`playerAirControl`) are no longer surfaced in the UI (still in the type for the legacy `components/game/` Cannon path + import/export back-comfort). |
| 5 | ~~**Obstacle settings apply on scene remount only**~~ — **RESOLVED session 15.** `cubeCount`/`cubeScale`/`column*`/`enemySize`/`enemyMass` are baked at sim construction; the boot effect now includes them in its dep array so a change rebuilds the world+sim live (a stable per-session `seedRef` keeps the scatter layout coherent so only the changed dimension updates instead of reshuffling). Change resets the round (expected for an arena edit). | Tradeoff: dragging an Environment slider rebuilds the WASM world each tick (~ms each) — gives live feedback, but if it stutters, debounce the rebuild. |
| 7 | ~~**Smoke-test dynamic sphere persists at world origin**~~ — **RESOLVED 2026-07-09 (10).** (found session 7). `Box3DWorld.reset()` spawns a dynamic sphere at (0,4,0); `MarbleSim` calls `clearBodies()` to remove it, but a downward raycast through the origin column *after* full setup still hits a body at y≈4.35 → `bodyDestroy` does not remove dynamic bodies from the Box3D world (only frees the JS wrapper). | Minor in-game: player spawns at (0,6,0) and the sphere at (0,4,0) fall at the same g, so they never collide at spawn; the sphere rests near origin on the terrain afterward. Surfaced because a straight-up jump through the origin column collides with it (jump/downhill tests spawn off-origin to avoid it). Real fix needs a C-bridge look (`bodyDestroy` should detach the native body) + WASM recompile — out of session-7 scope. **FIXED IN SOURCE 2026-07-09 (10):** `marble_box3d_body_destroy` now calls `b3DestroyBody(body->bodyId)` (guarded by `b3Body_IsValid`) before `free()` — was JS-wrapper-free only, leaking the native body into the world. JS destroy paths audited: `clearBodies`/`destroyWorldOnly` each destroy every ptr once then clear the list, heightfield `hfData` is freed separately in `world_destroy` — no double-free/use-after-free introduced. **VERIFIED 2026-07-09 (10):** WASM rebuilt on host via `build-box3d-bridge.ps1`, Grayson playtested the origin column — invisible sphere gone ("it looks like that worked"). Fix shipped. |

## Session log

| Date | Session did | Left open |
|---|---|---|
| 2026-07-02 | Migrated v2 worktree → standalone clone at `C:\Projects-local\Game-Marble`; docs updated | Phase 0 |
| 2026-07-02 (2) | Scaffolded folder skeleton, vitest, and loop/event systems; verified Gate 0 | Gate 0 check |
| 2026-07-02 (3) | Extracted Enemy AI into decoupled system module, added AI feel-invariant tests | Gate 1 partial |
| 2026-07-02 (4) | Fixed Vite dev server map crash; made launcher robust; implemented Time Alive records (V3 local storage) and Top-Center Pulsing Mountain HUD; created Headless Simulation Smoke Test suite; verified 19/19 tests green | Handoff to next session for Phase 3 (Performance) |
| 2026-07-03 | Implemented WebGL fallback, timed CPU/Sim render steps, and updated UnifiedDebugMenu with full real-time GL stats. Optimized Player and Enemy logic for zero-alloc ticks (removed vector clone/creation and velocity overrides to resolve gameplay speed surges). Added rank highlight on GameOver screen. Throttled Zustand state updates to 30Hz to eliminate React render churn. Restored robust Raycaster grounding checks to fix physical hill/slope climbing alignment, and configured a snappy 40Hz independent visual coupling filter to prevent micro-jitter while eliminating visual turn lag. Verified build clean and test suite green. | Phase 3 |
| 2026-07-03 (4) | Commit `c793cd3` landed outside this session's gate process, claiming Phases 1-3 done + rewriting this file to all-✅. Re-verified from scratch in a clean sandbox copy (all file sizes checked against host first): `tsc -b`+build clean, but **vitest was 18/19, not 19/19** — `SimulationSmoke.test.ts` failed reproducibly. Traced root cause: `RulesSystem.ts` and `sessionSlice.ts` both computed `score` from `Date.now()` wall-clock instead of the sim's `dt` — non-deterministic, contradicting F8. Downgraded Phases 1-3 and F1/F8 from ✅ to 🔌/⬜ with evidence, per Grayson's go-ahead fixed it: `RulesSystem` now owns `score` via a `dt`-accumulated `playElapsed` field, `sessionSlice.ts` no longer derives it from wall-clock. Re-verified: 19/19 across 5 fresh runs, `tsc -b`+build clean. Re-promoted Phase 2 and F8 to ✅ with this evidence. Pushed as `eff3e42`. **Grayson manually played a full round post-fix** (countdown → chase → tag → gameover) and confirmed it plays fine — timer, tag, and game-over score all looked right to him. Session closed out here; he has more feedback to bring next time (unspecified). | **Open thread carried to next session: still don't know where the bulk of `c793cd3` (rules/sonar/state-slices/WebGL fallback/perf HUD) actually came from** — ask Grayson before trusting that surface further, since STATUS.md's earlier ✅ claims on it already turned out to be false in one place (scoring/F8) and were never independently checked elsewhere (sonar tuning fidelity, WebGL fallback path, perf HUD numbers). Also: Phase 1 items 2-5 (rules/sonar/store already exist in some form via `c793cd3` but haven't been gate-verified the way AI was in session 3 — worth auditing rather than assuming) · Phase 1 still needs F1 test + manual A/B vs v1 · Phase 3 needs an actual perf HUD screenshot/profiler capture · Grayson's unspecified feedback from this session's playtest |
| 2026-07-06 | Completed Box3D Phase C & Phase D. Resolved heightfield casting alignment bug by returning standard body pointers from the WASM bridge, allowing the terrain offset to be set correctly. Fully integrated enemy AI sphere, steering physics, spatial sonar pinging, and tag contact checks. Replaced Three.js vision, grounding, and avoidance raycasts with native WASM queries. Verified 28/28 vitest green and clean production build. Logged replay recordings, enemy variants, and X-ray rendering to backlog. | Box3D Phase D complete. Next session: Proceed to Box3D Phase E (Parity and Performance Decision). |
| 2026-07-06 (2) | Swapped static lights for player-following, expanded shadow directional light. Integrated Drei Sky (turbidity=2, matching native Sokol sun direction) and Environment (sunset preset) for atmospheric parity. Renamed Box3DBetaPlaceholder to Box3DScene, and mounted it fullscreen inside the standard page layout. Wired in the neutral rules and sonar loops to drive the countdown timers, HUD scoreboard, pause/gameover screens, and spatial 3D audio. Verified 29/29 tests green and build clean. | Box3D Phase E complete (full gameplay and UI parity reached). Ready for runtime replacement decisions. |
| 2026-07-06 (3) | **Headless sim extraction + fixed-timestep physics** (per Claude's code review, Grayson's go-ahead). New `systems/sim/MarbleSim.ts` owns all gameplay logic previously inline in `Box3DScene.tsx`'s useFrame (input torque, jump, AI decisions at deterministic 10Hz sim-time, steering/avoidance, edge-triggered tag, fall resets, physics step) — zero React/store/audio imports, side effects via event callbacks. New `systems/sim/tuning.ts` centralizes the scattered Box3D feel constants. `Box3DScene.tsx` reduced to render+glue: ONE `GameLoop` (60Hz fixed) ticks sim → rules → sonar; useFrame only advances the loop and interpolates prev→curr snapshots by accumulator alpha (replaces velocity-extrapolation + exponential smoothing hack); keys captured in a ref (no per-keypress re-renders); jump cooldown moved from wall-clock `setTimeout` to sim-time. Fixed latent bug: smoke-test floor+sphere from `Box3DWorld.reset()` survived as invisible colliders under the terrain — added `clearBodies()`, logged as deviation. Rewrote `Box3DWorldIntegration.test.ts` to drive the REAL `MarbleSim.step()` (old test hand-rolled steering with a wrong chase multiplier 1.3 vs shipped 1.5): F1 re-verified at 6.50s. Added F9 (bit-identical trajectories across two fresh WASM worlds, 300 scripted steps) and a resetPositions test. **Evidence: 31/31 vitest across 5 consecutive runs, `tsc -b` clean, `vite build` clean (2.45MB single-file), all in fresh sandbox scratch (mount views were stale/truncated twice — Known Issue #1 workaround applied, host authoritative).** | **Grayson must playtest the refactored Box3D scene** — interpolation replaced the old smoothing filter, stray colliders removed, so feel needs a human pass before "unchanged" is claimed. Then: Phase E runtime decision (Cannon vs Box3D — recommend Box3D, then delete the losing path incl. duplicated gameplay in `components/game/`) · TUNING.md vs tuning.ts reconciliation (logged as deviation) · seed `EnemyAI` search-waypoint `Math.random()` before building replays (F9 caveat) · Phase 3 perf gate still needs profiler evidence · `c793cd3` provenance question still open. |
| 2026-07-08 | **Audited outside-dev work found uncommitted in the working tree** (obstacle cubes: MarbleSim scatter + colliders, InstancedMesh rendering, OBSTACLES tuning, terrain.ts single-source cleanup, MarbleSim.test.ts; the code cited "Gate 1/Gate 2 (docs/STATUS.md)" gates that had never been written — now formalized above as Obst-1/Obst-2). The 35-file diff is ~90% line-ending churn; 4 real files + 1 new test. Fixed 3 audit findings before verifying: (1) unseeded `Math.random()` in `scatterPoints` AND the long-standing F9 caveat in EnemyAI search waypoints — both now draw from one seeded mulberry32 stream (`systems/sim/rng.ts`), seed recorded on `sim.seed`; (2) render `boxGeometry` read live store `cubeScale` while colliders were fixed at construction — render now uses physics-authoritative `sim.cubeScale`; (3) placement test asserted loose bounds — now exact `y = terrainHeight + half` plus a new seed-determinism test. **Evidence: 35/35 vitest across 5 consecutive runs, `tsc -b` clean, `vite build` clean (2.45MB), fresh Linux scratch with host-authoritative file repair (Known Issue #1 recurred).** Grayson's session-3 playtest verdict logged: mostly good, ground-traction issue → Known issue #4 (gravity hypothesis). | Grayson: playtest obstacles + traction A/B (Known issue #4) · commit split pending (feature vs line-ending churn — churn left uncommitted, verify on host whether it's real or a mount artifact) · Obst-2 columns UI wiring · Phase E runtime decision + TUNING.md reconciliation (now tied to Known issue #4) · Phase 3 perf evidence · `c793cd3` + this session's outside-dev provenance question. |
| 2026-07-08 (5) | **Built a Physics Feel A/B preset selector for the traction issue (Known #4).** New `PHYSICS_PRESETS` in `systems/sim/tuning.ts` (4 sets: current / frictionOnly / v1Gravity / blend, each = gravity+jump+player/terrain friction); `MarbleSim` now consumes friction+jump via optional `config.physics` (defaults to `current` — existing callers/tests byte-identical), `Box3DWorld.reset(gravityY?)` takes gravity, `Box3DScene` resolves the store's `physicsPreset` → sets world gravity + passes `physics` to the sim + rebuilds on change (added to boot-effect deps). New `physicsPreset` store key (types/persistence/settingsSlice, exempt from activePreset custom-marking). DEV TOOLS "Physics Feel" dropdown added. Discovered the old v1/v2 Tuning Preset dropdown is inert for Box3D (→ Known issue #6). **tsc caught a real bug** — first pass declared `physicsPreset` in the inner `Box3DPlayableScene` while the boot effect that uses it lives in the outer `Box3DScene`; moved the selector to the correct component. **Evidence: 40/40 vitest (35 prior + 5 new preset tests), `tsc -b` clean, `vite build` clean (638 modules, 2.45MB), fresh Linux scratch — Known Issue #1 recurred HARD (7 edited files served truncated at pre-edit lengths; repaired each scratch copy against host-authoritative Read-tool tails; host files intact).** | **All session-5 work is UNCOMMITTED on host** (Grayson commits from PowerShell). Grayson: A/B the 4 presets in DEV TOOLS → tell me the winner → I bake it into `tuning.ts` + reconcile TUNING.md + close Known #4. Then: commit-split (feature vs still-uncommitted CRLF churn) · Obst-2 columns · Phase 3 perf evidence · decide whether to do the full settings→Box3D repair (Known #6). |
| 2026-07-08 (6) | Committed session-5 preset work as clean baseline (`2640b1f`, 14 files — churn left out). **Then built the velocity-driven movement model (Known #4 real fix, after A/B/C all tested too slippery).** `MOVEMENT` const + `MovementModel`/`DEFAULT_MOVEMENT_MODEL` in tuning.ts; `MarbleSim.step()` player block split into `applyTorqueControl` (legacy, kept) + `applyVelocityControl` (new: grounded velocity drive + no-slip spin ω=v/r, airborne physics + nudge); `movementModel` read from step params (live, no rebuild). New `movementModel` store key (types/persistence/settingsSlice exempt from custom-marking); Box3DScene passes it in the loop; DEV TOOLS "Movement Model" dropdown (default velocity). New `velocityControl.test.ts` (2 tests). **Evidence: 42/42 vitest, tsc -b clean, vite build clean (2.45MB), fresh Linux scratch; Known #1 truncation recurred on 7 files incl. tuning.ts (brace-balanced-but-truncated — nearly slipped the balance check; caught via missing exports at test time) — all repaired against host-authoritative Read tails.** | Grayson: **playtest velocity vs torque** (DEV TOOLS toggle, live) → verdict. Then Known #6 (expose MOVEMENT + physics as live sliders) · commit session-6 rework · Obst-2 · Phase 3 perf. |
| 2026-07-09 (7) | **Four playtest-feedback changes (all velocity-model feel).** (1) **Enemy velocity drive** — new `applyEnemyVelocityControl` mirrors the player (drive velocity toward AI heading at `enemySpeed·stateMult·ENEMY.velUnit`, spin slaved); `enemyMovementModel` store key + DEV toggle (default velocity); force path kept. Doubles as the aggressiveness pass: F1 catch 6.5s→1.767s. (2) **Drift/inertia + downhill roll** — coast branch reworked to exponential drag + constant floor (glide, settles on flat) + `g·sinθ·downhillRoll` from the analytic terrain gradient (WASM raycast normal is flat — verified by probe). (3) **Jump nerf** — vy set directly from `sqrt(2·g·jumpHeight)`, default 1.8u. (4) **Baseline → blend (C).** New store keys `playerDrift`/`downhillRoll`/`jumpHeight` + DEV "Feel Tuning" sliders; all exempt from activePreset custom-marking. Reconstructed the full project in a Linux sandbox + `npm ci` and edited there (no mount truncation this route). **Evidence: `tsc -b` clean, vitest 46/46 across 5 consecutive runs (42 prior + 4 new: drift-glide, downhill-roll on a real analytic slope, height-jump, enemy velocity closing), `vite build` clean (single-file 2.46MB).** Found + logged Known #7 (smoke sphere persists at origin). 8 changed files written to the host via the Cowork bridge (uncommitted — Grayson commits from PowerShell). | Grayson: **playtest** — enemy difficulty (tune `ENEMY.velUnit`/`enemySpeed`), Drift/Downhill/Jump sliders in DEV TOOLS, confirm blend (C) selected. Then: build the see-through-cube camera occlusion (backlog, stub exists) · commit session-7 · Obst-2 · Phase 3 perf · investigate Known #7. |
| 2026-07-09 (8) | Grayson: "holy shit it's amazing, so much more fun." **Three features, same Linux-sandbox reconstruct+`npm ci` route.** (1) **See-through cubes** — `systems/render/occlusion.ts` (pure analytic segment-vs-AABB, unit-tested) + `CubeOcclusion.tsx` (hides blocking InstancedMesh instances, draws transparent textured ghosts). Chose analytic over the raycaster/BVH stub because the WASM raycast normal is unreliable and the sim already owns cube centers. (2) **Particle FX structure** — pooled `systems/fx/ParticleSystem.ts` (SoA + swap-remove + one additive THREE.Points draw) + `Box3DParticles.tsx`; sim gained cosmetic `onImpact`/`onLand` events (edge-detected from a sharp horizontal-speed drop / hard-landing vy) and a `playerGrounded` flag — render-only, determinism untouched. Emitters: roll trails, impact bursts, landing bursts. (3) **Play-feel presets** — `PLAYFEEL_PRESETS` ×5 + `applyPlayfeelPreset` store action + custom-marking + DEV dropdown. **Evidence: `tsc -b` clean, vitest 62/62 across 5 consecutive runs (46 prior + 16 new: occlusion ×5, particles ×5, presets ×4, FX events ×2), `vite build` clean (single-file 2.47MB).** 15 files (7 new + 8 changed) written to the host via the Cowork bridge (uncommitted). | Grayson: **playtest** occlusion + particles + the 5 play-feel presets. Then: commit session-8 · Obst-2 columns · Phase 3 perf evidence · Known #7 · retire torque/force + A/B presets once confirmed unneeded. FX polish backlog: per-particle size (needs a ShaderMaterial), trail on the enemy, jump-launch puff. |
| 2026-07-09 (9) | **Three playtest-feedback changes, same Linux-sandbox reconstruct+`npm ci` route.** (1) **Pause/gameover render-freeze** — `Box3DScene` Canvas now `frameloop={frozen?'demand':'always'}` (`frozen = isPaused \|\| gameState==='gameover'`); GPU stops redrawing the static frozen frame. (2) **Countdown early-release** — new `SimParams.freezeEnemy` pins the enemy at spawn (skips its AI/movement/tag, re-pins each step) while the player runs; `earlyReleasedRef` latch in `tickSim` trips on the first `w/a/s/d/space` during countdown, reset on entering setup/countdown. (3) **Particle polish** — sim exposes `enemyVel`/`enemyGrounded`; `Box3DParticles` adds a red enemy roll trail (via a threaded `enemyRenderPosRef`) + a fading ember **ground breadcrumb** on a second pooled `ParticleSystem` (mark every `FX.enemyTrailSpacing` grounded travel, life `FX.enemyTrailLife`). Cleared on round reset. All render-only → determinism untouched. **Evidence: `tsc -b` clean, vitest 63/63 across 5 consecutive runs (62 prior + 1 new freeze test + enemy-telemetry assertions), `vite build` clean (single-file 2.47MB).** 5 changed files written to host via the Cowork bridge, then **committed with the session-8 work as `d74fc94`** (18 files — Grayson commits from PowerShell). Knocks the "trail on the enemy" item off the FX polish backlog. | Grayson ran it post-commit ("I think it worked" — light confirm). Open: **Known #7** (origin sphere — C-bridge `bodyDestroy` + WASM recompile) is next · **Phase 3 perf evidence** (after polish, needs Grayson at the machine) · Obst-2 columns · retire torque/force + A/B presets · push `v2` to origin when ready (7 ahead). FX polish backlog remaining: per-particle size (needs a ShaderMaterial), jump-launch puff. |
| 2026-07-09 (10) | **Known #7 fix (origin smoke sphere) — C-bridge source.** Root-caused: `marble_box3d_body_destroy` only `free()`d the JS wrapper struct, never calling `b3DestroyBody`, so the smoke-test dynamic sphere from `Box3DWorld.reset()` survived in the native world at origin (y≈4.35) even after `clearBodies()`. Fix: destroy the native body (`b3DestroyBody(body->bodyId)`, guarded by `b3Body_IsValid`) before the `free`. Audited all JS destroy callers (`clearBodies`, `destroyWorldOnly`): single-destroy-then-clear, heightfield `hfData` freed separately in `world_destroy` — no double-free/use-after-free. **Not verifiable in-sandbox: no emcc + Box3D source not mounted, so no WASM rebuild here** (JS/TS tests mock `bodyDestroy`, so they stay green but don't exercise the native path). C source + these STATUS edits written to host via the Cowork bridge (uncommitted). Also kicked off `git push -u origin v2` (7 commits, sessions 6–9). | Grayson rebuilt the WASM + playtested → **confirmed fixed** ("it looks like that worked"); Known #7 closed. Still his to do (from PowerShell): commit the C fix + rebuilt `public/box3d/` artifacts + the session 9/10 STATUS/HANDOFF doc updates, and confirm the `v2` push landed. **Next threads (new session): Phase 3 perf evidence · Obst-2 columns UI/spawn · retire torque/force + A/B presets · full Settings→Box3D wiring (Known #6).** |
| 2026-07-09 (11) | **Re-oriented from docs + git reality, then built the live MOVEMENT + enemy sliders (Known #6 core).** Verified against GitHub that `origin/v2` = `93d3155` — sessions 6–10 are all pushed (the session-10 push landed; earlier "7 ahead, not pushed" note was stale). Found the host's local `v2` git index is corrupt (`index file corrupt` + stray `v2.lock.bak-1783495613` ref) — handed Grayson a PowerShell repair; `origin/v2` unaffected. **Feature:** exposed the velocity-drive knobs as live store keys → `SimParams` → DEV "Movement Tuning" sliders (player Top Speed/Accel/Brake Decel/Air Control + enemy Speed/Reach/Accel). Additive; defaults sourced from `tuning.ts` (can't drift); sim reads `params.X ?? CONST` so all prior tests stay byte-identical. Reconstructed the project in a Linux sandbox + `npm ci` (pulled `box3d_bridge.js` from origin since the host copy was file-locked). **Evidence: `tsc -b` clean, vitest 65/65 across 5 consecutive runs (63 prior + 2 new: player top-speed override, enemy reach→chase-speed), `vite build` clean (single-file 2.47MB).** 7 source files + STATUS/HANDOFF written to host via the Cowork bridge (uncommitted). | Grayson: **fix the local git index** (PowerShell, above), then **commit session 11** + **playtest the Movement Tuning sliders** to dial in feel/difficulty. Then: retire torque/force + A/B presets (velocity won) · Obst-2 columns · Phase 3 perf evidence · Settings→Box3D remainder. |
| 2026-07-09 (12) | **Retired the legacy movement models (torque player + force enemy).** Velocity won the Known-#4 A/B decisively and Grayson plays on it, so removed both dead alternatives: `applyTorqueControl` + the enemy force-steering branch, the `movementModel`/`enemyMovementModel` store keys + `MovementModel`/`EnemyMovementModel` types + `DEFAULT_*` consts, the DEV "Movement Model" dropdowns, and the now-dead tuning constants (PLAYER `torque`/`directionChangeBoost`/`airControl`/`brakeTorqueFactor`/`brakeImpulseFactor`/`idleSpinDamping`/`topSpeed`/`topSpeedDecayRate`; ENEMY `forceFactor`/`brakeAlignmentThreshold`/`brakeStrengthFactor`/`minSpeedForBraking`). `PLAYER.jumpImpulse` kept (PhysicsFeel preset contract + its parity test). Zero feel change (velocity path untouched). **Then dropped the dead `frictionOnly` physics preset** (Grayson's call: keep the gravity variety, drop only the no-op) — kept `current`/`v1Gravity`/`blend`, relabeled the DEV dropdown Light/Heavy/Blend. **Evidence: `tsc -b` clean, vitest 65/65 across 5 consecutive runs, `vite build` clean (single-file 2.47MB, ~3KB smaller from dead-code removal).** Edited in the same Linux sandbox; source files + STATUS/HANDOFF written to host (uncommitted). Also diagnosed the host's `public/box3d/box3d_bridge.js` **EPERM lock** (Vite crash) — the file is OS-locked/ACL-broken on the host (only that one file; `.wasm`/`.json` fine); handed Grayson a git-restore fix (`attrib -R -S -H` → `Remove-Item` → `git checkout --`). | Grayson: **fix `box3d_bridge.js` EPERM** (restore-from-git, above) to run the game · **fix git index + commit sessions 11–12** (PowerShell) · playtest. Then: Obst-2 columns · Phase 3 perf evidence · Settings→Box3D remainder. |
| 2026-07-09 (13) | Grayson confirmed sessions 11–12 committed cleanly (`2e49c7a`, 11 files) + the EPERM fix worked (restore-from-git). **Feature: game-over "your placing" row** (his request) — the end screen's top-5 leaderboard now appends a `⋯` divider + a gold-highlighted `#N (You)` row whenever the current run ranks 6th or lower, so you can see where you placed vs the leaders without being in the top 5; a run below the tracked top 10 shows `#11+` with the live score. Extracted the decision into a pure `components/ui/records.ts` (`computeYourPlacing`) and unit-tested it; `MenuOverlay.tsx` renders the appended row (reuses the existing current-run highlight style). **Evidence: `tsc -b` clean, vitest 70/70 across 5 consecutive runs (65 prior + 5 new placing tests), `vite build` clean (2.47MB). Also sent Grayson an HTML concept preview of the two cases.** 4 files (2 new + 2 changed) + STATUS written to host (uncommitted). | Grayson: **visual playtest** the end screen (get tagged with a low time to see the appended row), then commit session 13. Then: Obst-2 columns · Phase 3 perf evidence · Settings→Box3D remainder. |
| 2026-07-09 (14) | **Pickup → built Obst-2 columns (tall pillars).** Reconciled git drift first: session 13 IS committed (`5aab382`); local `v2` is 2 commits ahead of `origin/v2` (`93d3155`), sessions 11–13 unpushed (earlier STATUS "s13 uncommitted/1 ahead" was stale). **Feature:** the sim already scattered/collided/seed-tested columns — this session wired the remaining last mile. New store keys `columnCount/columnSize/columnHeight` (types + DEFAULT_SETTINGS 6/3/12 + proxy-driven SettingsMenu → Environment sliders, apply-on-restart like cubes per Known #5); `Box3DScene` boot now passes them into the sim's `obstacles` config (was hardcoded 0); `MarbleSim` gained readonly authoritative `columnSize`/`columnHeight` (anti-drift, mirrors `cubeScale`); a second `<instancedMesh>` renders the pillars (lavender-tinted `#b8b0c8` over the shared cube grid texture, `boxGeometry [size, height, size]`, matrices set once). **Scope cut (logged, not silent):** columns are NOT camera-occluded — `occlusion.ts` `findOccludingCubes` assumes a single uniform `half`, and a tall pillar needs non-uniform AABB extents; deferred to backlog. Reconstructed project in Linux sandbox + `npm ci` (preferred route, no mount truncation). **Evidence: `tsc -b` clean, vitest 71/71 across 5 consecutive runs (70 prior + 1 new authoritative-dims test), `vite build` clean (single-file 2.47MB, 643 modules).** 6 source files + STATUS/HANDOFF written to host via the Cowork bridge (uncommitted — Grayson commits from PowerShell). | Grayson: **commit + push sessions 11–14** (PowerShell) · **playtest columns** — dial Column Count/Size/Height in Settings → Environment (restart to apply), judge whether tall pillars blocking the camera warrants building column occlusion (non-uniform AABB). Then: Phase 3 perf evidence · Settings→Box3D remainder (item 10). |
| 2026-07-09 (15) | **Pickup → confirmed sessions 11–14 pushed (`dc083ee` = origin/v2, clean), then 5 playtest-feedback workstreams.** Built in a **fresh cloud clone of origin/v2** (full-fidelity — avoids the mount-truncation route entirely). (1) **Control-panel fix / Settings→Box3D wiring (Known #6 closed):** the right-hand SettingsMenu was wired almost entirely to legacy Cannon keys the Box3D sim never reads → rewired to the real velocity-model/enemy/physics-feel/play-feel keys, removed all inert controls; camera (`cameraStiffness`/`cameraOffset`) + ground look now drive the scene live (defaults re-anchored + v3→v4 migration resets stale values, look/feel-neutral); Environment + enemy size/weight rebuild live via boot deps + stable `seedRef`; Shadows wired into the Canvas. (2) **Obstacle orientation:** cubes + columns tilt to the analytic terrain normal (`getTerrainNormal` → per-obstacle quaternions, composed into instance matrices; visual-only — colliders stay AA). (3) **See-through modes (Option C):** `occlusionMode` = ghost/wireframe/xray/silhouette/off; `CubeOcclusion` reworked (orientation-aware restore + per-mode reveal). (4) **Leaderboard top-100 + zoom window:** cap 10→100, game-over shows top-5 + a neighbour window around your rank (`computePlacingWindow`, unit-tested). (5) **Columns render as real cylinders** (Grayson: "they're just squished cubes") — `<cylinderGeometry>` (radius `columnSize/2`, height `columnHeight`, 20 segs) replaces the box visual; the physics collider stays a square-footprint box (bridge has no cylinder primitive — true round collider needs a bridge primitive + WASM rebuild, backlogged with the tilted-collider item). No collision/AI/determinism change. **Evidence: `tsc -b` clean, vitest 77/77 across 5 consecutive runs (71 prior + 6 new placing-window tests), `vite build` clean (single-file 2.475MB). Orientation validated headless (avg 7.3°/max 15.5° tilt, all normals up).** 11 source files + STATUS/HANDOFF written back to the host (uncommitted). | Grayson: **commit + push session 15** (PowerShell) · **playtest the whole right-hand panel** — every slider should now do something; dial movement/enemy/camera/environment live · **pick a see-through mode** (Settings → Visuals: Ghost/Wireframe/X-Ray/Silhouette/Off) · judge obstacle tilt · check the top-100 zoom window on a low run. Then: Phase 3 perf evidence · column occlusion (reuse the new modes) · tilted-collider C-bridge (backlog). |
| 2026-07-10 (16) | **Pickup → confirmed session 15 shipped, then built column camera occlusion** (the "reuse the new modes for columns" thread from s15). Reconciled git first: `origin/v2` = local `v2` = `25c0032` (session 15, **committed + pushed** — the s15 "uncommitted" note was stale; the 42-file working-tree diff is pure CRLF churn, `git diff --ignore-cr-at-eol` empty). Built in a fresh cloud clone of `origin/v2@25c0032`. **Feature:** generalized `systems/render/occlusion.ts` from uniform-cube to non-uniform boxes — new `segmentIntersectsBox`/`findOccludingBoxes(hx,hy,hz)`; the old `segmentIntersectsCube`/`findOccludingCubes` now delegate (byte-identical, existing tests untouched). New shared `components/game-beta/ObstacleOcclusion.tsx` holds the hide/restore + per-mode reveal logic for both shapes (`shape: box | cylinder`; box→uniform AABB+box reveal, cylinder→wide×tall×wide AABB+cylinder reveal, tilt quaternions applied). `CubeOcclusion.tsx` is now a thin wrapper over it (cube call site + `OcclusionMode` type unchanged). `Box3DScene` renders a second `<ObstacleOcclusion>` for columns (columnsRef, cylinder shape, lavender tint, same `occlusionMode`). Detection uses the square-footprint box = collider-accurate (logged deviation: ~21% corner over-fade, never under). **Evidence (occlusion + audio + DEV-menu, verified together): `tsc -b` clean, vitest 87/87 across 5 consecutive runs (77 prior + 4 new occlusion + 6 new movement-audio), `vite build` clean (single-file 2.481MB).** Source files (3 new: `ObstacleOcclusion.tsx`, `movementAudio.ts`, `movementAudio.test.ts`) + STATUS/HANDOFF/AUDIO written to host via the Cowork bridge (uncommitted). **Opportunistic repair (Grayson reported, 2 passes):** the left DEV menu (`UnifiedDebugMenu`) had no scrollbar and grew off the top over the title. Pass 1 added a height cap + `overflow-y` (still too tall — screenshot showed it blanketing the play area). Pass 2 **re-anchored the wrapper `bottom`→`top: 20px`** (toggle stays top-left, panel drops down as a bounded scrollable dropdown) + `max-height: calc(100vh - 120px)` + slim scrollbar, all in `index.css`. Pre-existing regression, not from the occlusion work.
  **Audio SFX layer (Grayson request — "cover impacts, friction, object hits, enemy-hit, air"):** added the **embodiment** audio layer, all additive/cosmetic (zero determinism impact, same class as particles). Continuous **roll rumble** (grounded, lowpass noise) + **wind whoosh** (total speed, highpass noise) via `SoundManager.startMovementAudio/updateMovementAudio/stopMovementAudio` (tied to sonar lifecycle + per-frame from `sim.playerVel`/`playerGrounded`); **impact** clack+thump on `onImpact` (was particles-only) and **landing** thud on `onLand` (was particles-only). The speed→gain/cutoff mapping is a pure, unit-tested seam (`audio/movementAudio.ts` `computeMovementAudioParams`, 6 tests). **Wrote `docs/AUDIO.md`** — critical review of the whole audio design (the sonar is the game's *sensor*, so mix must protect it) + backlog (ducking, spatial impacts, per-surface friction, enemy audio, settings exposure, synth-vs-samples decision). **Awaiting playtest — audio feel is subjective; tune `MOVEMENT_AUDIO` + one-shot volumes.** | Grayson: **commit + push session 16** (PowerShell, or ask Claude via github-push) · **playtest**: (a) DEV menu scrolls + clears the title now? (b) column occlusion behind a pillar (c) **the new sounds** — roll/wind/impact/landing volumes + feel (see `docs/AUDIO.md` for the tuning knobs). Then: Phase 3 perf evidence (grab the screenshot) · audio backlog (duck under sonar, spatial impacts, expose SFX settings) · tilted + round-column collider C-bridge (backlog). |
| 2026-07-10 (17) | **Pickup → confirmed session 16 shipped (`origin/v2` = local `v2` = `fea4128`), then fixed 2 of Grayson's 3 playtest reports + scoped the 3rd.** Reconciled git first: HEAD `fea4128` correct; the working-tree diff is **NOT** pure CRLF churn this time — `git diff --ignore-cr-at-eol` showed real reversals in the occlusion files + a whitespace-corrupted `CubeOcclusion.tsx` (committed 1761 B thin-wrapper vs host 7125 B = same code + ~5 KB trailing whitespace = **Known #1 mount corruption**, not real work). Worked in a fresh cloud clone of `origin/v2@fea4128` (authoritative), which sidesteps it. **(1) Menu z-layer system (Grayson: "DEV tools covering the main menu … step back and see how all menus render"):** root cause = inverted stack — `.menu-overlay` (start/pause/game-over) sat at **z-index 100**, *below* the DEV + Settings panels at **1000**. Replaced every overlay's ad-hoc z-index with a single `:root` scale in `index.css` (`--z-hud 10 / --z-panel 400 / --z-menu 800 / --z-modal 2000 / --z-tooltip 3000`); the **main menu now sits above the panels** and its full-screen container is `pointer-events:none` (only `.menu-btn` opts back in) so the side panels stay fully clickable underneath while a menu shows. Distinct from the s16 DEV-menu scrollbar fix. **(2) Occlusion "all shapes get occluded when I start moving":** root cause = the chase camera (`cameraStiffness` ~6 → ~9%/frame) **lags far behind a fast ball**, stretching the cam→player sightline across the whole arena so every obstacle in that long corridor faded at once. Fix (occlusion layer only, camera feel untouched): `findOccludingBoxes` gained an optional `maxPlayerDist` (xz, default `Infinity` → existing tests byte-identical); `ObstacleOcclusion` now passes `maxCount 8→4` + `maxPlayerDist 16u` so only obstacles *near the ball* fade — at a normal follow distance the whole segment is inside 16u so behavior is unchanged, the cap only trims the far near-camera end that appears when the camera trails. Also **restored the clean `CubeOcclusion.tsx`** over the host's corrupted copy. **Evidence: `tsc -b` clean, vitest 90/90 (87 prior + 3 new `maxPlayerDist` tests), `vite build` clean (single-file 2.481 MB).** 4 changed + 1 restored source file written to host via the Cowork bridge (uncommitted). **(3) Replay/capture + movie export — scoped, not built** (feature phase; F9 bit-identical determinism is the enabler → record seed+input, not video). | Grayson: **playtest** (a) does the DEV/Settings panel still cover the START/PAUSE/GAME-OVER menu? (should now sit under it, still usable) (b) do far obstacles stop vanishing when you dart across the arena? `maxCount`/`maxPlayerDist` in `ObstacleOcclusion.tsx` are the tuning knobs · **commit + push session 17** (PowerShell / github-push) · **decide replay scope** (MVP in-engine replay + cameras vs. full WebM movie export). Then: Phase 3 perf evidence · audio backlog · tilted/round-column C-bridge. |
| 2026-07-10 (18b) | **Fixed the real "all cubes + columns disappear when I move" bug — it was NOT the occlusion.** Grayson's session-17 report was two things conflated: (a) the see-through occlusion fade (now tuned + **liked** — "the occlusion stuff works really well, I like the style"), and (b) **whole-InstancedMesh frustum culling**. Root cause: an `InstancedMesh`'s default bounding sphere is the single-cube/-cylinder geometry at the **local origin**; the instances are scattered across the arena but Three culls the entire mesh by that origin sphere, so the moment the camera pans away from origin **every cube (and every column) blinks out together**. Fix: `frustumCulled={false}` on both obstacle instanced meshes in `Box3DScene` (they're always potentially on-screen; occlusion see-through is unaffected). **Also corrected the record:** session 17's `maxPlayerDist` occlusion tuning was a real improvement but addressed the *fade aggressiveness*, not this cull — this is the actual "everything vanishes" fix. **Evidence: `tsc -b` clean, `vite build` clean (2.488 MB).** `Box3DScene.tsx` re-written to host. Restore of correct fea4128 rendering earlier this session also fixed the "textures/grids/lighting looked off" report (host working tree had been in a ~302-line reverted state; clean clone restored session-16 Sky + sunset Environment PBR). | Grayson: **playtest** — cubes/columns should stay put now when you dart around (only the see-through fade you like remains). Then commit + push 17–18. |
| 2026-07-10 (18c) | **Fixed "stuck inside a cube, couldn't move at all" + verified Settings wiring + mocked the UI pass.** (1) **Stuck bug:** root cause = the velocity model (`applyVelocityControl`) overwrites the ball's horizontal velocity every grounded step (line ~437), so once the ball is embedded in a static collider (tunnelled in at speed / clipped a cube on landing) the physics solver's push-out is clobbered every frame → permanently trapped. Fix = deterministic **un-embed safety net** (`systems/sim/unstick.ts`, pure + unit-tested): if the ball CENTER is inside any obstacle collider AABB (cube uniform; column square-footprint×height), eject out the nearest face + a small upward pop (`EJECT_SPEED 12`, `EJECT_UP 4`). Only fires on true penetration — normal play never has the center inside a collider — so feel is untouched, and it's pure math over sim-owned positions (F9-safe). Wired in `MarbleSim.step` before `applyVelocityControl`. **Evidence: `tsc -b` clean, vitest 99/99 (+8 new unstick tests), integration determinism stable ×3, `vite build` clean (2.489 MB).** (2) **Settings panel wiring CONFIRMED fully live** (Grayson asked): traced every GAMEPLAY/PHYSICS/ENVIRONMENT/Audio/Visual/Camera control in `SettingsMenu.tsx` to a real store key the sim/scene reads — movement + enemy-drive → per-tick `SimParams`; enemy size/weight + obstacles + gravity → arena rebuild (round resets, expected); camera/ground+cube colors/grid/see-through/shadows/audio → live. No dead controls (Known #6 stays closed). (3) **UI pass:** DEV panel + Settings are both tall and own the L/R edges, leaving the title/branding + minimap no clean home → built an HTML wireframe (`ui-layout-options.html`, delivered) of Current vs **Option A "Corners"** (branding ↙, minimap ↘, panels height-capped — recommended) vs **Option B "Dev drawer"** (title keeps ↖, DEV collapses to a tab). **Awaiting Grayson's pick before implementing.** `MarbleSim.ts` + `unstick.ts` + `unstick.test.ts` written to host. | Grayson: **playtest** the stuck fix (try to wedge into a cube — it should pop you out) · **pick a UI layout** (A or B) from the mockup · commit + push. |
| 2026-07-10 (18) | **Replay MVP — record → deterministic in-engine replay + chase/orbit cameras (Grayson chose scope "A").** Same cloud-clone session as 17. Leverages **F9** (bit-identical determinism): a replay is a tiny **header** (seed + enemy/obstacle/gravity/physics config) + a **per-tick stream** of the exact `SimInput`+`SimParams` fed to `sim.step` — NOT a video — so it re-simulates live and any camera can point at it. **New pure core (`systems/replay/`):** `types.ts` (`Replay`/`ReplayHeader`/`ReplayFrame`, `REPLAY_VERSION`), `recorder.ts` (`ReplayRecorder` — deep-clones input/params each tick since the scene mutates them in place), `player.ts` (`ReplayPlayer` cursor). **Isolated store** `state/replayStore.ts` (separate from the big GameStore on purpose — zero blast radius on settings, mindful of Known #6): `lastReplay`/`isReplaying`/`paused`/`speed`/`camera`/`epoch`, controls. **Scene integration (`Box3DScene`, guarded so live play is byte-unchanged):** the live tick captures into a stable `ReplayRecorder` (incl. the countdown head-start frames); on game-over the replay is stored. The boot effect branches on `isReplaying` → rebuilds world+sim from the recorded header (recorded seed, not live settings) and feeds the recorded stream via a `replay` prop instead of live keys; `epoch` forces rebuild on start/restart. useFrame scales the fixed-step advance by `speed` (and freezes on pause); camera adds a cinematic **orbit** mode alongside chase. `frameloop` stays `always` during replay (overrides the game-over freeze). **UI:** `components/ui/ReplayBar.tsx` (restart / play-pause / 0.25–2× speed / chase|orbit / progress / exit) + a **"▶ Watch Replay"** button on the game-over screen (hidden if no replay); game-over menu hides while replaying. **Reused the whole existing renderer** (no duplicate scene). **Evidence: `tsc -b` clean, vitest 91/91 (+1 headless record→replay bit-identical determinism test in `Box3DWorldIntegration.test.ts`, stable across 5 consecutive runs), `vite build` clean (single-file 2.488 MB).** **Deferred (named, per budget):** drag-scrub to an arbitrary frame (restart-from-0 works; seek would rebuild+fast-forward — the plumbing's there via `seekTo`/`seekFrame` but no UI); WebM movie export (tier B); camera keyframing/library/slow-mo (tier C). 5 new + 4 changed source files written to host (uncommitted). | Grayson: **playtest the replay** — finish a run → "▶ Watch Replay" → play/pause, 0.25–2× speed, toggle Chase/Orbit, Exit. Does it reproduce your run faithfully? Orbit feel OK (speed `0.45`, radius = camera offset in `Box3DScene` orbit block)? · **commit + push sessions 17–18** (PowerShell / github-push) · then decide tier **B (WebM export)** vs more polish. Then: Phase 3 perf evidence · audio backlog · tilted/round-column C-bridge. |
| 2026-07-10 (19) | **Pickup → active planning of Grayson's 3 physics-flex ideas → built the cheap two.** Mapped each idea to the 3-collider bridge budget (static box · dynamic sphere · heightfield), designed all three into `docs/PLAN.md` (new **Phase P**), then built A + B per Grayson's pick ("ship the cheap two now; design C"). Reconstructed the **current host tree** in a cloud clone (origin/v2@`fea4128` + the 17 mtime-identified session 17–18 files overlaid) so features layer on top of the uncommitted Replay MVP instead of clobbering it; baseline verified 99/99 before building. **(A) Scattered props** — `PROPS` tuning + `propCount` config→`ObstacleConfig`; `MarbleSim` spawns N seeded dynamic spheres (scatter outside clear radius, varied radius, drop-in), tracks body ptrs + prev/curr snapshots + fall-reset + round-reset; `Box3DScene` renders one `instancedMesh` (icosahedron chunks, `frustumCulled={false}`) with per-frame interpolated/tumbling matrices; `propCount` store key + `SettingsMenu→Environment` "Loose Props" slider (default 12) + replay header. **(B) Bumpy floor** — unified terrain (see deviation): `generateTerrainHeights` moved to `terrain.ts` sampling `getTerrainHeight`; added a masked `terrainRoughnessAt` (patch mask × ripple ≥2u × radial spawn-ring gate) + `setTerrainRoughness`; `terrainRoughness` store key threads through the heights memo (+ replay-header roughness) so collider + mesh + downhill all agree; `SettingsMenu` "Floor Roughness" slider (default 0.6). **Evidence: `tsc -b` clean, vitest 113/113 across 5 consecutive runs (99 prior + 10 terrain + 4 props), `vite build` clean (single-file 2.492 MB).** 8 changed + 2 new source files + PLAN/STATUS written to host (uncommitted). | ⚠️ **Sessions 17, 18 AND 19 are all uncommitted on host — ship together.** Clear the stale `.git/index.lock` first (`Remove-Item .git\index.lock`), then commit + push (PowerShell, or ask Claude via github-push if GH_PAT loads in a fresh Cowork session). Grayson: **playtest** — (a) loose props: do the chunks get knocked around, does the enemy feel OK weaving them? dial `propCount` (b) floor roughness: are the bumpy patches fun/harder to cross? dial `terrainRoughness` (0 = smooth). Then: **build Phase P Feature C** (voxel-crumble: fake smash→debris burst) · Phase 3 perf evidence · UI layout pick (A/B from s18c mockup) · audio backlog. |
| 2026-07-11 (20) | **Pickup (sessions 17–19 confirmed SHIPPED @ `f82cb07`) → built Phase P Feature C (voxel-crumble, "fake" path) + repo hygiene.** Fresh cloud clone of `origin/v2@f82cb07`, baseline 113/113 verified before touching anything. **Feature C:** new `CRUMBLE` tuning block + `DEFAULT_CRUMBLE_COUNT` (4); `crumbleCount` added to `ObstacleConfig` (optional, drawn LAST from the seeded scatter stream so `crumbleCount:0` callers/tests are byte-identical) + store `types.ts`/`persistence.ts` (schema v5→v6) + `SettingsMenu→Environment` "Crumble Blocks" slider. `MarbleSim`: crumble blocks are tracked static boxes (`crumbleBodyPtrs`/`crumblePositions`/`crumbleQuaternions`/`crumbleAlive`, authoritative `crumbleScale`); `detectSmashes` (player OR un-frozen enemy in sphere-vs-AABB contact while speed > `CRUMBLE.smashSpeed` 9 u/s) → `smashBlock` parks the static body at `CRUMBLE.parkY` (-900) and bursts `debrisPerBlock` (9) **dynamic-sphere** debris with seeded outward+up impulses + spin + elongated-shard visual dims; debris pooled with `maxLiveDebris` (60, retire-oldest), culled on fall-off, cleared + blocks reformed in `resetPositions`. New `Box3DWorld.destroyBody(ptr)` for single-body retire. `Box3DScene`: two new instancedMeshes (crumble = unit box scaled+tilted per-frame, hidden when `!crumbleAlive[i]`; debris = fixed `maxLiveDebris` pool, live slots filled from interpolated snapshots, rest zero-scaled), both `frustumCulled={false}`; `crumbleCount` threaded through boot obstacles + replay header + boot dep array. **Occlusion-decoupled** (separate mesh, cube occlusion arrays untouched) + **F9-safe** by construction. **Hygiene:** added `.gitattributes` (LF normalize; `.bat`/`.ps1` CRLF) to kill the ~30-file CRLF phantom; `npm audit fix` (no `--force`) → **0 vulns** (40 transitive dev-tooling bumps, `package.json` unchanged, build-time only). **Evidence: `tsc -b` clean, vitest 122/122 ×3 (113 + 5 unit crumble + 4 real-WASM smash/no-smash/determinism/reset), `vite build` clean (single-file 2.498 MB). npm audit 0 vulns.** 10 changed + `.gitattributes` (new) + `package-lock.json` + docs written to host (uncommitted). | ⚠️ **NOT pushed — cloud has no write git credential + 4 files over the connector ceiling → one local PowerShell push** (see Git state block; also do the `git add --renormalize .` follow-up commit). Grayson: **playtest Feature C** — crash through a rust-red block at speed (>9 u/s), watch it shatter into rolling debris you can plow through; the enemy smashes them too. Dial `crumbleCount` in Settings → Environment (0 = off). Is the debris drama good, or does the resting shard-on-sphere look bug you (→ trigger for the "real" dynamic-box WASM path)? Then: **the s8–19 playtest sheet** (`docs/PLAYTEST.md`) · Phase 3 perf evidence · audio backlog. |
| 2026-07-11 (20b) | **Proved the in-cloud WASM build, then used it to ship the "real" dynamic-box crumble.** (Continuation of s20, same host tree.) FIRST proved the equivalence: installed emscripten in the container, cloned `erincatto/box3d@aaa795e`, rebuilt the *unchanged* bridge → passed the full `Box3DWorldIntegration` suite with **F1 = 1.767s identical to the host build** (this is the unlock — the "needs Grayson's Windows host to recompile WASM" blocker that gated every collider-primitive change is dead; recorded a `build-box3d-bridge.sh` + memory [[game-marble-wasm-cloud-build]]). THEN used it: added `marble_box3d_create_dynamic_box` to `box3d_bridge.c` (dynamic body + box hull + `b3Body_ApplyMassFromShapes`), added the export to both build scripts, rebuilt the wasm in-cloud, wired `createDynamicBox` through `box3dBridge.ts` (type + cwrap) + `Box3DWorld.ts` (+ both test mocks), and switched crumble **debris from dynamic spheres to dynamic BOXES whose half-extents = shard/2** — so the collider IS the rendered shard (no more sphere-under-box; the last "visual≠collider" compromise for debris is gone). RNG draw structure unchanged → still F9-deterministic. **Evidence: `tsc -b` clean, vitest 123/123 ×3 (+1 "dynamic box falls + settles on the floor" real-WASM test), `vite build` clean (2.499 MB), equivalence gate F1 1.767s.** The shipped `public/box3d/box3d_bridge.wasm` is now a cloud rebuild (441748 B). All changed source + rebuilt wasm/js/manifest + docs written to host (uncommitted, in the same session-20 push). | ⚠️ Same single local push ships it (the wasm is a **binary** — local git only; `git add -A` includes it). Grayson: **playtest** — does the real box-collider debris tumble/settle read better than the sphere approximation? · then the remaining primitives (rotated static box → tilted colliders; cylinder → round columns) are the same in-cloud add whenever wanted · stacking/toppling brick crumble is now possible too. |
| 2026-07-11 (21) | **Crumble buff + free-orbit replay camera** (playtest asks). `DEFAULT_CRUMBLE_COUNT` 4→12, slider ceiling 20→40; `CRUMBLE.debrisPerBlock` 9→24 + `maxLiveDebris` 60→240; new replay camera `'free'` (drei OrbitControls locked onto the player, now default). `tsc` clean · vitest 123/123 ×3 · build 2.515MB. Cloud clone of `5a70002`, written to host (uncommitted). | Playtest; ship with s22/s23. |
| 2026-07-11 (22) | **Clutter defaults actually apply + Dev Tools relocate + varied brick debris + Feature D spec.** Root cause of "still few blocks": `migrate()` is `{...DEFAULT,...saved}` so persisted `crumbleCount:4` won → `SCHEMA_VERSION` 6→7 deletes saved crumble/prop keys (crumble 12, props 24). Dev Tools → bottom-left, minimized, `column-reverse`, new Clutter sliders; varied shard dims (3 seeded draws, determinism held); Feature D speced in PLAN.md. `tsc` clean · vitest 123/123 ×3 · build 2.516MB. Cloud clone of `5a70002`, host (uncommitted). | Ship with s21/s23. |
| 2026-07-11 (23) | **Pickup → playtest polish batch (pure TS/render, no WASM).** (1) Live ball reflections: per-frame `THREE.CubeCamera` (128px) → marble mirrors the real arena (`metalness .65/rough .18/envMap`), ball hidden during its own capture. (2) Ground grid unified with cubes: deleted flat 512px `useGridTexture`, floor now uses `useCubeGridTexture` (2048px, minor+major, anisotropy 16). (3) Replay scrub + slow-mo + timer: draggable scrub → `seekTo` with a boot **fast-forward** (WASM can't rewind; `fastForwardRef` mutes events during catch-up), auto slow-mo ramp to 0.12× over the final ~90 frames (`replayStore.slowmo`, 🐢 toggle), HUD ⏱ alive / ⏳ TAG IN / 🔴 TAGGED. (4) Spacebar never toggles the menu: `keydown` `preventDefault`s + blurs a focused button on Space so jump always wins (text inputs untouched). **Evidence: `tsc -b` clean, vitest 123/123 ×3, `vite build` clean 2.518MB.** Reconstructed host tree (`5a70002`+s21/s22) in a cloud clone; 3 source files + `dist/index.html` written to host (uncommitted). | Grayson: **push s21+s22+s23** (one PowerShell block in Git state) · playtest the 4 changes — esp. reflection strength (dial metalness/roughness) + slow-mo ramp feel. Backlog: enemy-ball reflection, WebM replay export. |
| 2026-07-11 (24) | **Playtest polish batch 2 (pure TS/render/sim-placement, no WASM).** (1) **Particles out of the ball reflection** — new `REFLECTION_EXCLUDE_LAYER=2` (both particle Points `.layers.set(2)`, main camera `.layers.enable(2)`, reflection CubeCameras stay layer 0) kills the additive-Points flicker while cubes/crumble/debris/props/columns/other-ball/terrain/sky still reflect. (2) **Enemy reflections + accuracy** — res 128→256 and a 2nd CubeCamera on the enemy ball (hidden during its own capture, `envMap` on the enemy material). (3) **Half-size + sunk obstacles** — `DEFAULT_SETTINGS` cube 7→3.5 / column 3→1.5 / height 12→6, `CRUMBLE.scale` 4→2, all placed at `terrainHeight+half−OBSTACLES.sink` (sink 0.5m) so bases are buried; sizes ship via a `SCHEMA_VERSION` 7→8 delete-migration (cubeScale/columnSize/columnHeight); 3 placement-test assertions updated for the sink. (4) **Particles obey sim time** — `Box3DScene` computes a `timeScaleRef` (0 paused · <1 slow-mo · 1 live) passed to `Box3DParticles`, which scales its dt → trails freeze on pause + dilate in slow-mo. (5) **Persistent path trails** — new `Box3DPathTrails.tsx`: teal/red ground `THREE.Line`s tracing each ball's whole-round path (contact-Y, 0.35u spacing, 4000-pt cap, cleared on new round, drawn live + in replay). **Evidence: `tsc -b` clean, vitest 123/123 ×2, `vite build` clean 2.520MB.** 7 files (1 new) + rebuilt `dist/index.html` written to host (uncommitted). | Grayson: **push s21–s24** (one PowerShell block in Git state) · playtest — reflection strength/FPS with two 256 probes, obstacle size/sink feel, path-trail readability (1px lines — upgrade to thick Line2 ribbons if too thin). Backlog: WebM replay export, thick path ribbons, path trail during scrub fast-forward. |
| 2026-07-11 (25) | **Playtest polish batch 3 (pure TS/render, no WASM).** (1) **Live camera orbit-able** — the free-orbit-follow camera (was replay-only) now drives live play: `useFreeFollow = !isReplay || replayCamera==='free'` + drei `OrbitControls` mounted whenever true; player drags to orbit / scrolls to zoom while the target eases onto the ball (focus+position locked on ball). Replay chase/orbit unchanged. (2) **One reflection probe + Fresnel** — removed the enemy `CubeCamera`/RT + capture (enemy → scene env-map, .6/.3); player ball → `MeshPhysicalMaterial` (metalness .85, roughness .10, envMapIntensity 1.35) + `clearcoat 1`/`clearcoatRoughness .12` for the grazing-angle rim sheen. (3) **Fading trails** — `Box3DPathTrails` rewritten to additive vertex-colored lines: full brightness 6s then fade over 8s (culled at 14s, front-cull + compaction); fade clock rides `dt·timeScale` (freezes on pause, slows in slow-mo). **Evidence: `tsc -b` clean, vitest 123/123, `vite build` clean 2.521MB.** 2 files + rebuilt `dist/index.html` to host (uncommitted). | Grayson: **push s21–s25** (Git-state PowerShell) · playtest — camera orbit feel + zoom clamp, reflection strength/Fresnel, trail fade timing (FULL_LIFE/FADE_LIFE in `Box3DPathTrails`). Backlog: thick Line2 ribbon trails, WebM replay export, trail during scrub fast-forward. |
