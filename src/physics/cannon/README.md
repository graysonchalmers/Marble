# Cannon Backend

The current playable game uses `@react-three/cannon` directly inside React/R3F components.

This folder is reserved for a future adapter if we decide to normalize Cannon and Box3D behind one TypeScript interface. For the first Box3D beta pass, current Cannon code stays where it is so the default runtime remains stable.

